# 官方 LINE Bot 用戶旅程（A 軌 + B 軌 + 銜接鉤子）

> **每個 session 接官方 Bot 工作先讀這份**。不要再問「這個流程怎麼走的」。
> 跟 code 有出入時以 code 為準 + 立刻更新這份。
>
> 最後更新：**2026-04-23 深夜**（Phase 4.1 Session 2/3 全收工 + 3 件 prod incident 全修 + landing v4.2 + stage=4 bridging。詳見 `交接_2026-04-23_Session2-3+事件收工.md`）

---

## ⚡ 現在（2026-04-23 Phase 4.1 Session 2/3 + 3 incident 修完後）prod 長這樣

| 狀態 | 值 | 備註 |
|------|-----|------|
| TEST_MODE | **`false`**（全量開放）| route.js:72，2026-04-23 PR #29 切 |
| B 軌 Q1→Q4 | ✅ 全量用戶可走 | Phase 3.3 邏輯 |
| **stage=1 自由文字 fallback** | ✅ PR #35 | q1_retry_weight template |
| **stage=2 體重重傳（修正）** | ✅ PR #38 | UPDATE DB + 重推 Q2 + path_choice（KCw incident）|
| **stage=2 其他自由文字 fallback** | ✅ PR #35 | 輕量引導（許莎拉 incident）|
| **stage=4 Q4 後自由文字 → handoff bridging** | ✅ PR #40+41 | triggerHandoff + fifi 訊息 + notify 一休+婉馨。**Phase 4.2 Q5 wire 後拿掉** |
| Q5 軟邀請 stage=6/7/8 | ❌ 未 wire | Phase 4.2 才接（helper + routes 備好）|
| /apply landing | ✅ **v4.2 全上 prod（PR #39）**| Ch.2 科學段 + Ch.4 四大系統+市面比拼+三大堅持 + Ch.5 雙人早鳥+6 月班 |
| **/api/apply/visit** | ✅ Phase 4.1 Session 1（PR #32）| 驗 HMAC → stage 6→7 + q5_click_count + q5_clicked_at COALESCE |
| **/api/apply/submit** | ✅ Phase 4.1 Session 1（PR #32）| 驗 HMAC → submit_application RPC → stage=8 |
| **HMAC signed URL** | ✅ lib/q5-apply-url.js helper 備好 | buildQ5ApplyUrl / verifyQ5ApplySig 共用 |
| **applications 表** | ✅ migration_013 | 19 欄不加 UNIQUE（家庭共用 LINE）|
| **submit_application RPC** | ✅ migration_014 | PL/pgSQL，含 IF NOT FOUND RAISE P0002 |
| handoff notify | ✅ push 給一休 + 婉馨 | lib/handoff.js:16-19 |
| Supabase q5_* 欄位 | ✅ 9 新欄全建 | migration_011（blocked_at）+012（q5_sent/intent/classified_at 等 5 欄）+013（q5_click_count/clicked_at/visit_followup_sent_at 3 欄）|
| Vercel env | ✅ HMAC 3 個全設（Prod + Preview）| Q5_APPLY_SIGNING_SECRET_V1 / KEY_VERSION=1 / MAX_AGE_SEC=86400 |

---

## 漏斗總圖

```
┌─────────────────────────────────────────────────────────────┐
│  abc-website (abcmetabolic.com)                             │
│    代謝測驗 → 拿到 claim_code（寫入 quiz_sessions）         │
└──────────────────────────┬──────────────────────────────────┘
                           │ 用戶複製代碼
                           ↓
┌─────────────────────────────────────────────────────────────┐
│  官方 LINE Bot（@sososo）加好友                             │
│                                                             │
│  ┌─────────────────────────────────────────┐                │
│  │ 入口 1：follow 事件                      │                │
│  │   → handleFollow → 歡迎訊息             │                │
│  └─────────────────────────────────────────┘                │
│                                                             │
│  ┌─────────────────────────────────────────┐                │
│  │ 入口 2：傳 4 碼代碼                      │                │
│  │   webhook/route.js:85-92                │                │
│  │   /^[A-Z2-9]{4}$/ → handleCodeClaim     │                │
│  │   【繞 TEST_MODE】全量用戶可用          │                │
│  │   ↓                                     │                │
│  │   個人化代謝報告（依 metabolism_type）  │                │
│  │   + 自動進 Drip 排程                    │                │
│  └─────────────────────────────────────────┘                │
│                                                             │
│  ┌─────────────────────────────────────────┐                │
│  │ A 軌：關鍵字自動回覆（route.js:85-158） │                │
│  │   【繞 TEST_MODE】全量用戶可用          │                │
│  │                                         │                │
│  │   關鍵字 → 對應 handler                 │                │
│  │   報告 / 代謝報告 → handleReport        │                │
│  │   地雷 → handleTrap（末尾問體重）      │ ★ 銜接鉤子 1    │
│  │   菜單 → handleMenu（末尾問體重）      │ ★ 銜接鉤子 2    │
│  │   說明會 → handleSeminar                │                │
│  │   文章 → handleArticle                  │                │
│  │   ABC / 瘦身 → handleABC                │                │
│  │                                         │                │
│  │   handler：matchKeyword 命中直接 reply  │                │
│  │   不進對話路徑 dispatch                 │                │
│  └─────────────────────────────────────────┘                │
│                                                             │
│  ┌─────────────────────────────────────────┐                │
│  │ Handoff pre-check（route.js:98-138）    │                │
│  │   stage >= 2 用戶先走：                 │                │
│  │   Level 0 禮貌結束（「不需要」「謝謝」）│                │
│  │   Level 1 Handoff 關鍵字                │                │
│  │     want_enroll / asked_price /         │                │
│  │     asked_family → triggerHandoff       │                │
│  │     【繞 TEST_MODE】                    │                │
│  └─────────────────────────────────────────┘                │
│                                                             │
│  ────────── TEST_MODE gate（route.js:162） ──────────       │
│  if (TEST_MODE && !TEST_ALLOWLIST.includes(userId)) return; │
│  上面繞 gate 的路徑之外，以下對非白名單全部靜默             │
│  ──────────────────────────────────────────                │
│                                                             │
│  ┌─────────────────────────────────────────┐                │
│  │ B 軌：對話路徑 Q1→Q4（Phase 3.3）       │                │
│  │   handleConversationPath                │                │
│  │                                         │                │
│  │   Q1 / Q2（stage 0/1）→ 體重數字        │                │
│  │     extractWeights 抓到 → 進 Q2         │                │
│  │     pickWeightDiffCondition 分          │                │
│  │     小差距 / 中 / 大                    │                │
│  │                                         │                │
│  │   Q2（stage 2）→ 選 A/B/C/D             │                │
│  │     CHOICE_TO_PATH → healthCheck /      │                │
│  │     rebound / postpartum / eatOut /     │                │
│  │     other                               │                │
│  │     複選 AB/ABD → 引導選單一            │                │
│  │                                         │                │
│  │   Q3（stage 3）→ 選 1/2/3/4             │                │
│  │     q3_choice + q3_condition_selected   │                │
│  │                                         │                │
│  │   Q4（stage 4）→ AI 綜合回饋            │                │
│  │     generateFinalFeedback（Gemini）     │                │
│  │     path_all_q4_feedback 模板           │                │
│  │     視角：我=一休 / 你=用戶             │                │
│  │     寫入 ai_tags.q4_classified_at       │                │
│  └─────────────────────────────────────────┘                │
│                                                             │
│  ┌─────────────────────────────────────────┐                │
│  │ Q5 軟邀請（Phase 4.2 未 wire）          │                │
│  │   stage=4 後再傳文字 → Q5 classifier    │                │
│  │   q5_intent=continue → pushQ5SoftInvite │                │
│  │   Quick Reply「看看做法」→ /apply       │                │
│  │   Quick Reply「有問題想問」→ handoff    │                │
│  └─────────────────────────────────────────┘                │
└─────────────────────────────────────────────────────────────┘
```

---

## A 軌 → B 軌銜接鉤子（容易忘，寫死）

**關鍵事實**：A 軌的「菜單」「地雷」回覆 template **末尾會問**「告訴我你目前幾公斤、想瘦到幾公斤」。

這是 A 軌把用戶**主動推**到 B 軌 Q1-Q2 的設計 — **不是婉馨人工做的**，是 Bot 自動化的。

設計意圖：
- 用戶對「地雷 / 菜單」有興趣 = 有瘦身意願
- 趁熱問體重 → 進 B 軌 Q1-Q2 → 收集資料 → Q3-Q4 給個人化回饋

### 銜接點的 code 流程

1. 用戶傳「菜單」→ route.js:141 `matchKeyword(text)` 命中
2. route.js:154 呼叫 `rule.handler(userId)` → 回覆 template（末尾「問幾公斤」）
3. 用戶看到後回「50.9 47」→ 下一則訊息進 webhook
4. 不是 4 碼代碼、不是關鍵字 → 進 TEST_MODE gate（route.js:162）
5. **TEST_MODE=true**：非白名單被擋 → 靜默 → 漏斗斷
6. **TEST_MODE=false**：進 handleTextMessage → handleConversationPath → extractWeights 抓數字 → 進 Q2

### 銜接鉤子的 template 位置

- 關鍵字規則：`lib/keywords.js`（先讀 DB `official_settings`，fallback 到 code）
- 回覆文案：`official_settings` 表（後台可編輯）
- A 軌 handler：`app/api/webhook/route.js` handleMenu / handleTrap 等

---

## 當前開放範圍矩陣（2026-04-23 Phase 4.1 Session 1 後）

| 功能 | 觸發條件 | 對非白名單 | 備註 |
|------|---------|----------|------|
| 代碼領取 | 4 碼代碼訊息 | ✅ 開放 | handleCodeClaim |
| A 軌關鍵字 | 菜單/地雷/報告等 | ✅ 開放 | matchKeyword |
| Handoff pre-check | stage≥2 + 關鍵字 | ✅ 開放 | 觸發 push 給一休+婉馨 |
| Drip 排程推播 | cron */10 | ✅ 開放 | 後台啟用文章 |
| 一般推播 | admin API | ✅ 開放 | |
| **B 軌 Q1→Q4** | stage 0-4 + 文字 | **✅ 開放**（TEST_MODE=false 後）| PR #29 2026-04-23 切 |
| /apply 頁 | liff.line.me 連結 | ✅ 能開 | LIFF 簡化版（不驗 userId）|
| **/api/apply/visit** | POST 帶 HMAC signed body | ✅ 開放（需驗 HMAC）| Phase 4.1 Session 1 PR #32 |
| **/api/apply/submit** | POST 帶 HMAC + 表單欄位 | ✅ 開放（需驗 HMAC）| Phase 4.1 Session 1 PR #32 |
| Q5 軟邀請推送 | stage=4 後再傳 or cron | ❌ 未 wire | Phase 4.2 才接 classifier + performQ5Transition |
| unfollow 處理 | unfollow 事件 | ✅ 開放 | markBlocked + blocked_at |

---

## 當前斷點（2026-04-23 Phase 4.1 Session 2/3 + 事件後狀態）

### ~~斷點 1：A→B 銜接在 TEST_MODE=true 下完全斷~~ ✅ 已解

2026-04-23 PR #29 切 TEST_MODE=false，B 軌全量開放。A 軌問體重 → 用戶回 → 現在會自動進 Q1→Q2→Q3→Q4 AI 回饋。

### ~~斷點 2：Q1/Q2 自由文字靜默~~ ✅ Phase 4.1 Session 2/3 完成

- PR #35：stage=0 matchKeyword 後 upgrade 到 1 + stage=1 retry_weight fallback + stage=2 weights/free-text fallback
- PR #38：stage=2 重傳體重要更新 DB（KCw incident）

### ~~斷點 3：Q4 後用戶回應 Bot 靜默 + 沒通知~~ ✅ bridging 上線（PR #40+41）

- **現象**：Q4 AI 回饋末尾「想不想聽聽她們當時是怎麼從這裡走出來的？」用戶回「好」/「想」 → 原本 stage=4 靜默 + 一休/婉馨沒 notify
- **bridging（Phase 4.2 Q5 wire 前臨時）**：stage=4 自由文字 → triggerHandoff(reason='q4_followup_before_q5_wire') → fifi 訊息 + notify 一休+婉馨
- **Phase 4.2 Q5 wire 後拿掉**（code 有 TODO 註解）

### 斷點 4：B→Q5 classifier 正式流未 wire ⏳ Phase 4.2

- **位置**：Phase 4.2 未實作
- **當前替代**：stage=4 bridging（見斷點 3）
- **解法**：Phase 4.2 接 Q5 classifier + performQ5Transition（helper 已備）+ 拿掉 bridging

### 斷點 5：handoff notify push 給一休 + 婉馨 ⚠️ 預期行為

- **位置**：lib/handoff.js:16-19 寫死 NOTIFY_USER_IDS
- **現象**：用戶觸發 handoff（想報名 / 多少錢 / 我老公反對 / Q4 後回應）→ 立即 push 通知
- **2026-04-23 後**：TEST_MODE=false，handoff notify 正常進一休+婉馨的 LINE
- **reason 對照表**：want_enroll / asked_price / asked_family / q5_followup / **q4_followup_before_q5_wire（新，PR #40）**
- **解法**：若要調整收件人，改 official_settings 的 `handoff_notify_to`

### ~~斷點 6：/apply LIFF userId 防分享做不到~~ ✅ Phase 4.1 Session 1 已改用 HMAC

PR #32：/api/apply/visit + /apply/submit 都驗 HMAC signed URL。

### ~~斷點 7：landing copy 五章 + 視覺 + 簡報整合~~ ✅ PR #39 v4.2 上 prod

- v4 copy 定版（4 輪 iteration）：Hero / 可能性（一休原話骨架）/ 確定感（5 學員）/ 心理錨點（四大系統+三大堅持+市面比拼）/ 方案+報名（12 週 / 4 週 / 雙人早鳥 $3,333 anchor + 6 月班）
- v4.1 視覺：Hero 漸層 / 章節 emoji 下劃線 / 學員卡 accent bar / anchor 卡數字圓圈 / 方案卡推薦徽章
- v4.2 整合：Ch.2 科學段（被鎖住/冷凍庫/附加價值）+ Ch.4 四大系統（知識/營養/運動/支持）+ 三大堅持 + 市面比拼表 + Ch.5 雙人早鳥 anchor
- 雙人早鳥不進 form enum，走人工 LINE

### 背景 TODO（一休有意識，但不急）

- **補推舊 333 被擋用戶**：延後到 Phase 4.2 Q5 完整做完才推
- **TEST_MODE 搬 official_settings**：後台可開關（契約 backlog）
- **stage=4 bridging 拿掉**：Phase 4.2 Q5 wire 上線後（code TODO 已標）
- **退費條款最終法務稿**：目前 landing 放一休的「對話版」+ 導連結官網 Q6，上線前確認可以

---

## Supabase 專案（重要）

- **三個 Bot 共用一個 Supabase 專案**：阿算 + 官方 Bot + 休校長小幫手
- Project ref：`fnlkhxnfaylhqhystmbr`
- Supabase Dashboard 顯示名「coach-line-bot」（歷史命名，勿以為分開）
- MCP 可直接用 `mcp__supabase__execute_sql` 操作（project_id: `fnlkhxnfaylhqhystmbr`），不用本地直連 script
- BRAIN.md 寫「阿算 Supabase vs 小幫手分開」是過時，已更新

---

## Schema 真相來源

見 `official-yihugh-line-bot/CLAUDE.md` 的「Supabase 資料表（Schema 對齊）」。
或直接 SQL：
```sql
SELECT column_name, data_type FROM information_schema.columns
WHERE table_name = 'official_line_users' ORDER BY ordinal_position;
```

# V3.2 完整測試 SOP（給一休）

> 部署：2026-05-04 凌晨，6 個 commit push main，Vercel Production Ready
> 範圍：表單改造 + 超早鳥定價 + 後台控制台

## 你拿到的東西

### 用戶端
- **LINE 名稱必填**（form 預填 LIFF 帶到的，可改）
- **LINE ID 選填**（既有，placeholder 改清楚）
- **退費條款 6 條 inline**（不再連到外部 Q6，用戶送出前一定看到）
- **三段價錨點**：~~$12,600~~ → 主價 $11,400 / $10,400 + 倒數計時

### 後台
- `/admin/reset-v32?secret=official-bot-2026` — 一鍵清自己 DB（既有）
- `/admin/super-early-bird?secret=official-bot-2026` — **超早鳥控制台（新）**
  - 顯示當前狀態（綠 = 超早鳥中 / 黃 = 已關）+ cutoff_at 台灣時區 + 距離 diff
  - 按鈕「⏹ 立刻關閉超早鳥」（cutoff = NOW）
  - DateTimePicker + 「✅ 套用截止日」（過去 / 未來都可）

### DB 改動（已 prod）
- `applications` 加 2 欄：`super_early_bird_applied` BOOLEAN + `final_price` NUMERIC
- 5 個 settings 預設值（cutoff = 2026-05-31T15:59:59Z = 5/31 23:59 +08）
- `submit_application` RPC 升 14 參數（INSERT 含新 2 欄）

---

## 測試流程

### Test 1：表單改造（LINE 名稱 + 退費條款）

1. 開 `/admin/reset-v32?secret=official-bot-2026` → 一鍵清自己
2. LINE 傳「85 → 70」走 Q1-Q4
3. 「想聽聽」→ 學員故事 Flex →「想了解 ABC」→ 訊息 1
4. 走完訊息 1/2/3 → 點 URI button 進 /apply
5. **預期看到**：
   - 退費條款 section 直接顯示 6 條 ol（不是「詳見官網 Q6」link）
   - 表單有「LINE 名稱 *」必填 input，預設帶你 LIFF 顯示名（可改）
   - 「LINE ID（選填）」placeholder 「如果有設定 LINE ID 請填，沒有也沒關係」
   - 12 週方案 plan card 顯示：~~原價 NT$ 12,600~~ → **NT$ 10,400**（紅色）+ 「🔥 超早鳥優惠中 · 剩 X 天 X 小時截止」
6. 把 LINE 名稱清空 → 按送出 → 應該紅字「請填 LINE 名稱」
7. 不勾退費 checkbox → 送出 → 紅字「請勾選同意退費條款」

### Test 2：報名實際走通 + DB + notify

1. 表單填齊（LINE 名稱填一個你想用的，例如「一休」）
2. 勾同意退費 → 送出
3. **預期收到 push**（自己 LINE）：
   ```
   📝 新報名通知
   姓名：[你填的]
   方案：12 週完整版（🔥 超早鳥）
   成交價：NT$ 10,400
   ...
   ```
4. SQL 驗 DB：
   ```sql
   SELECT real_name, display_name, line_id, program_choice, super_early_bird_applied, final_price
   FROM official_program_applications
   ORDER BY id DESC LIMIT 1;
   ```
   預期 `super_early_bird_applied = true` + `final_price = 10400.00`

### Test 3：後台 toggle 關閉 → 表單變常規價

1. 開 `/admin/super-early-bird?secret=official-bot-2026`
2. 看當前狀態應該是「🔥 超早鳥優惠中」+ cutoff_at = 2026/05/31 23:59
3. 按「⏹ 立刻關閉超早鳥」→ confirm → 看到「✅ 已更新 cutoff_at」+ 狀態變「⏸ 超早鳥已關閉」
4. 開 `/admin/reset-v32?secret=...` 清自己 → LINE 重走 Q1-Q4 → 進 /apply
5. **預期表單變**：
   - 12 週方案：~~原價 NT$ 12,600~~ → **NT$ 11,400**（深綠）+ 「12 週完整版 · 早鳥優惠」
   - 沒有倒數計時（cutoff 已過）
6. 提交 → 收 notify「方案：12 週完整版（常規早鳥）/ 成交價：NT$ 11,400」

### Test 4：DateTimePicker 設新截止日（延長）

1. 回 `/admin/super-early-bird` 控制台
2. DateTimePicker 選一個未來時間（例如 6/1 23:59）→ 「✅ 套用截止日」→ confirm
3. 重新載入頁面 → 狀態應該變回「🔥 超早鳥優惠中」+ 新 cutoff_at
4. /apply 應該又看到 $10,400

### Test 5：邊界測試

- DateTimePicker 選**過去時間** → confirm 警告「這個時間已經過了！設下去等於立刻關閉」→ 確定後變關閉狀態 ✓
- 後台改 cutoff 後，**已提交**的訂單 final_price snapshot 不變（DB 看舊訂單仍 10400 / 11400）✓

---

## 已驗證 DB（migration 跑完 prod）

| Check | 結果 |
|---|---|
| applications.super_early_bird_applied 存在 | ✓ |
| applications.final_price 存在 | ✓ |
| settings.super_early_bird_cutoff_at = 2026-05-31T15:59:59Z | ✓ |
| settings.price_12weeks_super = 10400 | ✓ |
| settings.price_12weeks_anchor = 12600 | ✓ |
| submit_application RPC 14 參數版本 | ✓ |

---

## 風險回退

如果發現任何用戶流程壞了：
```bash
# 先 rollback Phase 4-6（前端 + admin）
git revert 2bd0f7e 3dfd633 f6ac7cf
git push origin main

# 必要時 rollback Phase 3 + migration
git revert ccbd5c8 b737bb5
# migration rollback SQL 在 migration_018 檔案頂端 comment 裡
```

但理論上 0 影響一般用戶（試行 gate 還在 — 一般用戶按 q4_story_interested 仍走 polite reply + handoff）。

---

## 沒接的（待你測完批准後做）

- **拆 V3.2 試行 gate 全量開放**（`q4_story_interested` 入口的 `if (!TEST_ALLOWLIST.includes(userId))` 拿掉）
- Phase 2B Session 3：dispatcher priority + handleV32FreeText + apply ?from= 量測歸因
- Phase 2C：cron q5-maintenance 段 5/6/7（24h 推進救援）

測完都 OK → 我接 Session 3 + 拆 gate。

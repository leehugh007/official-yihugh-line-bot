# 官方 LINE Bot 交接指南 — 2026-04-20 並行戰略

> **明天 / 未來新 ss 開工前先讀這份**。含 A 軌 / B 軌開工方式、禁區、驗收條件。

## 快速脈絡（30 秒看懂）

今天跑了 5 輪技術 + 1 輪戰略 yi-challenge 產出對話路徑契約 v6。Baseline SQL 實測顯示回訊息率 5.9%（93.7% 用戶拿代謝報告就消失）。**不是契約方向錯，是觸發率低**。一休判斷：對話流程是基礎設施（不可避免），open loop 是流量閥門（放大）。**兩軌並行都要做**。

## A 軌：改代謝報告 open loop（快速 ROI）

### 開 ss 指令（擇一）
> 「**接 official-yihugh-line-bot，走 A 軌**」
> 或：「**接 official-yihugh-line-bot 改代謝報告 open loop**」

### 進來先讀
1. 本檔（交接指南）
2. `~/.claude/checkpoints/official-yihugh-line-bot/2026-04-20T16:50-並行戰略.md`（完整脈絡）
3. `.skills/yi-voice/SKILL.md`（語感系統）

### 要改的檔（3 個位置）
| 檔案 | 行號 | 改什麼 |
|------|------|--------|
| `lib/keywords.js` | 132-230 | `METABOLISM_REPORTS` 5 種類型的報告文案，**每種結尾加 open loop** |
| `app/api/webhook/route.js` | 849 | `buildPersonalizedReport(session, displayName)` 個人化組裝邏輯，若 open loop 需要動態變數（例如代謝類型名）要在這裡接 |
| `app/api/webhook/route.js` | 141-144 | follow event 觸發的報告推送（若要改觸發時機）|

### 5 種代謝類型（lib/keywords.js:132 起）
- `highRPM`（高轉速）/ `rollerCoaster`（雲霄飛車）/ `burnout`（燃盡型）/ `powerSave`（省電型）/ `steady`（穩定型）
- **每種的 open loop 要對應該類型的痛點 / 好奇點**

### Open loop 設計方向（待一休定奪）
- 問題式：「想知道你的代謝型態怎麼對應到飲食選擇嗎？回『菜單』我告訴你」
- 故事式：「同類型 87% 學員用這個月瘦下 X 公斤，回『案例』看他們怎麼做」
- 挑戰式：「你的代謝型態最怕的食物是 X，猜看看是什麼？回『地雷』我告訴你」

### 驗收條件
- 改完 push → 等 7-14 天 → 跑 baseline SQL 看新 cohort 回訊息率
- 目標：從 5.9% 拉到 15%+（2.5x）
- **到 15-20% 才啟動 B 軌 Phase 3（webhook dispatch 改動）**

### 禁區
- ❌ 不改 webhook/route.js 的 dispatch 優先權（那是契約 Phase 3）
- ❌ 不動 handleQuizCodeClaim 的代碼驗證邏輯（只改報告內容）
- ❌ 不提阿算、不提 $4,980 當月價（它是 4 週體驗、12 週完整是 $11,400）

---

## B 軌：Phase 2b 純 DB 層建置（不影響用戶，可今晚 / 明天並行）

### 開 ss 指令（擇一）
> 「**接 official-yihugh-line-bot，走 B 軌**」
> 或：「**接 official-yihugh-line-bot Phase 2b 純 DB 動手**」

### 進來先讀
1. 本檔
2. `official-yihugh-line-bot/契約_對話路徑.md`（v6 完整，特別是第 9/10/11 章 + 附錄 B）
3. `~/.claude/checkpoints/official-yihugh-line-bot/2026-04-20T16:50-並行戰略.md`

### 執行順序（照做）

**Step 1: migration_008a**
- 複製契約第 10.1 章完整 SQL
- 用 `mcp__supabase__apply_migration`（**不要**自己寫 BEGIN/COMMIT）
- 目標 project：`fnlkhxnfaylhqhystmbr`
- 驗證 SQL：契約第 10.1 章末列的 3 個 SELECT 都要過

**Step 2: 新建 5 個 lib 檔**（code 可直接 copy 契約）
- `lib/official-settings-defaults.js`（契約第 8.3 章）
- `lib/official-settings.js`（契約第 8.2 章 getSettingTyped + SETTING_SCHEMA）
- `lib/dynamic-templates.js`（契約附錄 D）
- `lib/templates.js`（renderTemplate，契約第 4.3 章）
- `lib/users.js` 加 `updateAiTags` / `updatePathStage` / `getUserPathState` / `mergeByOp`（契約第 9.1/9.2/9.3 章，ESM export）

**Step 3: app/api/health/route.js**
- 複製契約第 10.1.1 章 code
- 驗證：`curl https://official-yihugh-line-bot.vercel.app/api/health?contract=check` 應回 `{ok: true, mismatch: false, missing_settings: [], ...}`

**Step 4: scripts/test-helpers.mjs**
- 複製契約第 10.1 章末 dynamic import 版本
- 跑：`npm i -D dotenv && node scripts/test-helpers.mjs`
- 驗收 3 個 case（mergeByOp 去重 / _from_ai 英文→中文 / updatePathStage reset retry_count）
- 測完清 `DELETE FROM official_line_users WHERE line_user_id LIKE 'TEST_HELPERS_%'`

**Step 5: migration_008b seed 40 條**
- 從契約附錄 B 逐條 seed（文案來源照欄位指示抄 `Bot對話設計_2026-04-18_定版.md` 或 `附錄 F`）
- SQL 用契約第 10.2 章的 `ON CONFLICT DO UPDATE` 模板（**刻意排除** buttons / image_url / is_active）
- 驗收：`SELECT COUNT(*) FROM official_reply_templates = 40` + `WHERE is_active=true = 0`

**Step 6: Phase 2c 後置 VALIDATION SQL**
- 契約第 10.2 章末 3 個 check SQL

**Step 7: 全部 commit push**
- 包含 Phase 1 積欠未 commit 的檔（migration_007 / 定版檔 / CLAUDE.md modified / 指揮中心 modified）
- 加上 Phase 2 產出（契約_對話路徑.md + 交接_2026-04-20 + migration_008a/b + 所有新 lib）
- 推 `leehugh007/official-yihugh-line-bot` main → Vercel 自動部署
- 等 Vercel Ready → curl /api/health 驗證

### B 軌完成後的狀態
- DB 有完整 schema（新欄位 / settings / webhook_events 表 / 40 條模板 is_active=false）
- helpers 全部 ready（等 Phase 3 呼叫）
- /api/health 可驗證 contract_version=v6 + missing_settings 空
- **用戶零影響**（is_active=false 不觸發、新 helper 無人呼叫）
- Phase 3 啟動前所有前置都備齊

### 禁區
- ❌ 不改 `webhook/route.js`（那是 Phase 3）
- ❌ 不改對話流程文案（100% 照契約附錄 B 文案來源抄）
- ❌ 不啟用任何 template 的 `is_active=true`（等婉馨 FlexPreview + 一休確認）
- ❌ 不破 existing keyword 回覆（Phase 1 的欄位已落地，但 webhook 沒用到）

---

## Phase 3 啟動 gate（A 軌見效後）

**條件（全過才啟動）**：
- [ ] A 軌 open loop 上線 7-14 天
- [ ] 新 cohort 回訊息率 ≥ 15-20%（baseline 5.9% 的 2.5-3 倍）
- [ ] 一休確認「對話流程要接了」
- [ ] B 軌 Phase 2b 已完成（DB + helpers + /api/health ready）

**Phase 3 動作**（當時才做，現在不做）：
- 改 `webhook/route.js` 的 `handleTextMessage` dispatch 優先權
- 新增 stage check + global Handoff + on_keyword_match 分支
- 改 route.js 前必畫 if/else 結構圖（04-04 括號位移事故教訓）
- 跑 Phase 4 AI 分類（Gemini 2.0 Flash Lite）

---

## 今天 session 的核心產出

### 跨專案永久資產
1. `/yi-contract` skill（`.skills/yi-contract/SKILL.md` + `.claude/commands/yi-contract.md`）
2. `架構契約/踩過的雷.md`（一休整理的 5 大陷阱 + 自檢清單）
3. `.skills/yi-challenge/SKILL.md` 升級（Phase 0 強制讀踩過的雷 + Adversarial/Feasibility 加 8 pattern）
4. `.skills/yi-design-check/SKILL.md` 升級（Phase 3 強制觸發 yi-contract）
5. `.skills/yi-project-bootstrap/SKILL.md` 升級（Checklist 加初始契約）
6. `.claude/rules/task-routing.md` 加路由
7. `CLAUDE.md` Skill Commands 加 `/yi-contract`
8. `memory/feedback_contract_pitfalls_累積.md` + MEMORY.md 索引

### 官方 Bot 資產
1. 契約_對話路徑.md v6（五輪技術 + 一輪戰略挑戰 = 30+ 致命洞解完）
2. Phase 1 schema 已落地 Supabase（12 欄 + reply_templates 表）
3. 40 條模板清單 + migration_008a 完整 SQL + 所有 lib code 可 copy-paste

### 還沒 commit 的東西（Phase 2b 完成後整批推）
- Phase 1：migration_007.sql / Bot對話設計_2026-04-18_定版.md / 官方Bot開發建議與討論.md (untracked)
- Phase 1 modified：CLAUDE.md / 指揮中心.md
- Phase 2a：契約_對話路徑.md / 交接_2026-04-20_並行戰略.md
- （Phase 2b 時新增）：migration_008a.sql / migration_008b.sql / lib 新檔 / app/api/health / scripts/test-helpers

## 關鍵檔案地圖（給新 ss 快速定位）

```
official-yihugh-line-bot/
├── 契約_對話路徑.md          ← 本專案 Phase 2-7 的唯一真相來源
├── 交接_2026-04-20_並行戰略.md ← 本檔（明天開 ss 先讀）
├── CLAUDE.md                  ← 專案上下文（有 Schema 對齊表）
├── 指揮中心.md                ← 部門架構 + 最近異動
├── Bot對話設計_2026-04-18_定版.md ← Section 3 文案來源
├── lib/
│   ├── keywords.js            ← A 軌要改（METABOLISM_REPORTS 5 種類型）
│   ├── users.js               ← B 軌要加 helpers
│   └── （B 軌要新建 5 個 lib 檔）
├── app/api/
│   ├── webhook/route.js       ← A 軌可能要改組裝邏輯；Phase 3 才動 dispatch
│   └── health/route.js        ← B 軌要新建
└── supabase/
    ├── migration_007_conversation_paths.sql  ← Phase 1 已 apply（但檔案 untracked）
    └── （B 軌要新建 migration_008a + 008b）

架構契約/
├── 踩過的雷.md                ← 5 大陷阱經驗庫（yi-challenge/yi-contract 必讀）
└── system_git-old-backup清理.md ← 另一個 session 的契約（無關）

~/.claude/checkpoints/official-yihugh-line-bot/
└── 2026-04-20T16:50-並行戰略.md ← 最新 checkpoint，完整脈絡
```

## 注意事項

### Git 狀態（獨立 repo）
- 專案在 `/Users/leehugh007/AI_asis/official-yihugh-line-bot/`（獨立 git repo，branch main）
- AI_asis 本身不是 git repo（`.gitignore` line 17 ignore 掉）
- Remote: `leehugh007/official-yihugh-line-bot`
- HEAD: `4c0d36f`（3 月血糖 handler，Phase 1 後沒新 commit）

### Supabase
- project_id: `fnlkhxnfaylhqhystmbr`（跟阿算 / 小幫手共用）
- 用 `official_` prefix 隔離
- 改 schema 必先查 information_schema 確認欄位

### Vercel
- Project: `prj_EUcS8nb3GTcsgiYgQjdStILTrK9N`
- 部署：push main 自動
- Cron: `*/10 * * * *`（每 10 分鐘，B 軌若加 cron A/B/C 併入現有 `/api/cron/drip`）

### TEST_MODE
- 目前 `webhook/route.js:47` 硬編碼 `true`（只有白名單收到回覆）
- 契約第 10 章 Phase 7 才改為從 `official_settings.test_mode` 讀取
- B 軌不要動

---

*2026-04-20 交接產出。下次開工從「接 A 軌」或「接 B 軌」開始。*

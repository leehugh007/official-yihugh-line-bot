# 契約：結單漏斗 v3.2 自動推進

> 版本：v3（修 yi-challenge 第二輪 3 致命 + 8 重要 = 11 個洞）
> 最後更新：2026-05-03
> 狀態：待一休判定可進 Phase 2A（或第三輪挑戰）
> 對應提案：`official-yihugh-line-bot/提案_2026-04-30_結單漏斗v3.md`
> 對應訊息範本：`official-yihugh-line-bot/草稿_2026-05-03_訊息範本v1.md`（v3.2，4 個 path × 學員故事承接）
> 既有契約：`契約_對話路徑.md` v6.1 + `契約_Q5轉換漏斗.md` v2.4（並行不取代）

---

## 摘要

把學員故事 Flex「想了解 ABC 在做什麼」按下後「polite reply 給 fifi」改成「自動推 4 段訊息 → /apply 報名」。

**v3.2 服務範圍**（v3 必修 致命 2 補）：
- ✅ **服務 4 個 path**：healthCheck / rebound / postpartum / eatOut
- ❌ **不服務** path=other / path=NULL：q4_continue handler 對這類用戶**不推學員故事 Flex**（既有 webhook L292-345 邏輯），因此**永遠 reach 不到 q4_story_interested**，走原 fallback handoff（既有 Q5 軟邀請軌或 bridging）
- 量測 baseline 必須只看 4 path 用戶（path=other/null 排除在 v3.2 漏斗外）

**完整入口脈絡**（v2 補強，原 v1 寫錯）：
```
Q4 AI 個人化回饋
  ↓ 用戶按 Q4 Quick Reply「想聽聽」(postback q4_continue)
[既有 Phase 4.6] 推學員故事 Flex（path × 4 + Fallback Flex）
  ↓ 用戶按 Flex「想了解 ABC 在做什麼」(postback q4_story_interested)
[v3.2 入口] q4_story_interested handler 改寫 → 推訊息 1（不再 handoff、不再 polite reply）
```

**核心 4 段**：
1. **訊息 1（intro）**：承接學員故事 + 鋪陳 ABC（path × 4 + Fallback，**textMessage** + 3 postback Quick Reply）— v3 改：剛從學員故事 Flex 點過來，同張圖再放一次 = 重複，改 textMessage 簡化
2. **訊息 2（method）**：純介紹 ABC 三框架（path 中立、不講價、textMessage + Quick Reply）
3. **訊息 3（offer）**：12 週課程 + 三段價 + 早鳥 + URI 跳 /apply（path 中立）
4. **訊息 4（final）**：最後提醒（早鳥剩餘時間 + URI 跳 /apply，path 中立）

**每段三按鈕**：① 想看下一步 ② 我再想想 ③ 我有問題想問
**24h 沒按按鈕** → cron 自動推進到下一段
**全域節流**：一天最多推一則
**「我有問題想問」** → handoff fifi + reply 設定預期「fifi 會聯絡你，之後 Bot 不會自動回」+ 永久不再自動推
**「我再想想」** → 寫時間戳，永久不再自動推（不 handoff）；用戶日後傳意願詞會 reply + 重發 Quick Reply

---

## 7 個設計決策（拍板紀錄，不變）

| # | 決策 | 答 |
|---|------|---|
| 1 | 新軌跟舊軌怎麼區分 | B：用 q5_msg1_sent_at 當「進新軌」signal |
| 2 | 新欄位放哪裡 | A：top-level column（migration_017）|
| 3 | q4_story_interested handler 改成什麼 | B：直接走新路徑（拿掉 polite reply、不 handoff）|
| 4 | 新軌中 pre-check 攔截 | A：保留 |
| 5 | postback handler 結構 | B：routing table |
| 6 | /apply/visit handler stage 升級 | A：所有新軌結束 stage 都先升 6 再讓 visit 升 7 |
| 7 | cron q5-maintenance 7 段拆路由 | A：先合併觀察 |

---

## v3 修了什麼（yi-challenge 第二輪 11 洞 = 3 致命 + 8 重要）

| # | 洞 | 修在哪一章 |
|---|---|----------|
| **致命 1** | q5_apply_from_msg 設計斷裂（v2 把 from 放 URL query，但 landing extractHmacParams + visit BODY_KEYS 不讀 → 永遠寫不到）| Ch.5.6 補 5 處配套清單 + Ch.10.1 Phase 2D |
| **致命 2** | q4_continue 對 path=other/null 不推學員故事 Flex，v3.2 只服務 4 path | 摘要 + Ch.4.path 範圍 + Ch.5.0 入口補 + Ch.11.6 baseline |
| **致命 3** | clearV32Tracking + handoff_triggered_at 死鎖（reset 不清 handoff 欄位 → cron 永遠排除）| Ch.5.11 reset 清單擴充 |
| **重要 1** | reply token 衝突 invariant（pre-check 搶 token） | Ch.5.1 補 invariant |
| **重要 2** | partial postback failure 卡死（intro_next 寫 replied_at 後 push msg2 失敗 → cron 永遠不推）| Ch.5.2 順序倒置 + cron SQL 主軸改「q5_msg2_sent_at IS NULL」 |
| **重要 3** | 意願詞 negation 未處理（「不想」「不了解」誤判）| Ch.6.3 加 negation 規則 |
| **重要 4** | buildQuickReplyForStage helper 沒列（重發當前段 Quick Reply 需要）| Ch.2.5 + Ch.10.4 helper 清單補 |
| **重要 5** | reasonZh dict 對 q4_story_question / q4_story_maybe 沒中文化（fifi 看 raw key）| Ch.2.3 補 |
| **重要 6** | DoD baseline 寫「過去 1-2%」但實測 5/3 是 3/69 ≈ 4.3% | Ch.11.6 改 baseline |
| **重要 7** | Phase 0 文案盤點不完整（缺 16 條：12 button label + 4 maybe + 4 question reply + image URL）| Ch.10.2 補 |
| **重要 8** | Ch.5.4 reply「※ 之後 Bot 不會自動回」要過 yi-voice 自檢 | Ch.10.2 文案規則補 |

剩 3 尚可洞（ESLint rule 防 inline UPDATE / HMAC 48h 訊息 4 重簽 / Phase 0 文案 yi-voice 結尾簽名）→ Ch.11.7 backlog。

---

## v2 修了什麼（yi-challenge 第一輪 15 洞）

| # | 洞 | 修在哪一章 |
|---|---|----------|
| **必修 A** | 入口脈絡錯位（q4_story_interested 是學員故事 Flex 的 button，不是 Q4 AI 末尾入口）| 摘要 + Ch.5.0 |
| **必修 B** | stage=5 dispatcher 不存在（webhook L868 直接 return）| Ch.5.4 reply 文案 + Ch.6.5 |
| **必修 C** | 重啟路徑沒清 q5_msg* 欄位（Pattern 7）| Ch.5.11 + Ch.7.7 |
| **必修 D** | cron 段 5/6/7 SQL 誤撈 handoff 用戶 | Ch.5.9 SQL 改 path_stage = 4/IN(4,6) + handoff_triggered_at IS NULL |
| **必修 E** | URI button 不送 webhook click event | Ch.2.1 拿掉 q5_msg3_clicked_at |
| **必修 F** | 訊息 3/4 URI 跳同網址 → 點擊歸因斷 | URI 加 `?from=msg3/msg4` + 加 q5_apply_from_msg 欄位 |
| **必修 G** | stage=6 既有沒 dispatcher | Ch.5.10 改寫 |
| **必修 H** | pushFlexMessage 不支援 postback button | ~~Ch.2.6 補 buildMsg1Flex helper~~ → **v3.3 一休拍板訊息 1 改 textMessage**，buildMsg1Flex 不需要寫，Ch.2.6 廢棄 |
| **重要 I** | HMAC SIG_MAX_AGE_SEC 24h 過期 vs cron 推 msg4 | Ch.8 86400 → 172800（48h）|
| **重要 J** | 按 maybe 後傳意願詞被困死 | Ch.6.3 補意願詞偵測 |
| **重要 K** | 訊息 2/3/4 path 中立還是個人化沒明寫 | Ch.4 補「path 中立」|
| **重要 L** | 訊息 3 升 stage=6 但不寫 q5_sent_at 沒解釋 | Ch.2.2 補 NULL 比較證明 |
| **重要 M** | handoff notify 對 v3.2 沒對應 | Ch.6.2 補 v3.2 進度文字 |
| **重要 N** | nurturing pool 未定義（v4 黑洞）| Ch.5.7 標 🩹 v2 deadline |
| **重要 O** | cron 60s 沒安全邊界（132 人 P95 ~58s）| Ch.5.9 LIMIT 100→50 + console.time |

剩 4 尚可洞 → Ch.11.7 列為 v2.x backlog。

---

## Pattern 預先自檢

| Pattern | 對應防禦 |
|---------|---------|
| ① Enum 完整性 | Ch.2 + 附錄 A |
| ② 狀態機邊界 | Ch.5 + Ch.7 |
| ③ Race | Ch.5 + Ch.9 conditional UPDATE |
| ④ Placeholder | N/A — 無變數 |
| ⑤ Schema 可重跑 | Ch.10 |
| ⑥ AI 輸出 | N/A — 無 AI 分類 |
| ⑦ Code 環境 | Ch.10 ESM |
| ⑧ Cron / 平台 | Ch.5 + Ch.8 NOW() - INTERVAL + LIMIT 50 |
| ⑨ 止血 vs 根治 | 摘要 + Ch.5.7 nurturing 🩹 |
| ⑩ 目的反推 | Ch.2 + Ch.5 |

**Pattern 7（跨層原子性）特檢**（v2 強化）：同步位置 5 個（q5_msg{N}_*_at / q5_last_pushed_at / q5_apply_from_msg / path_stage / LINE push）+ 觸發源 3 個（cron 段 5/6/7 / postback handler / **stage=3 reset 路徑**）→ 抽公用 helper（Ch.10.4）。

---

## 第 1 章：狀態機總圖

### 1.1 v3.2 自動軌跟既有 stage 的關係

```
既有 stage 語意（不變）：
  0=未進對話路徑 / 1-3=Q1-Q3 / 4=Q4 完成 / 5=Handoff / 6=Q5 軟邀請已推（既有）/ v3.2 訊息 3/4 推送後 / 7=點過 /apply / 8=已 submit

v3.2 新軌的「中間態」用 q5_msg{N}_sent_at 當 stage proxy：
  stage=4 + q5_msg1_sent_at NOT NULL + q5_msg2_sent_at IS NULL → 「在訊息 1 等回應」
  stage=4 + q5_msg2_sent_at NOT NULL + q5_msg3_sent_at IS NULL → 「在訊息 2 等回應」
  stage=6 + q5_msg3_sent_at NOT NULL + q5_msg4_sent_at IS NULL → 「在訊息 3 等回應」
  stage=6 + q5_msg4_sent_at NOT NULL                          → 「在訊息 4 等回應」
```

### 1.2 4 段訊息推進流程

```
[Q4 AI 個人化回饋] ← 既有 Phase 3.2c
    ↓ 按 q4_continue（既有 Phase 4.6）
[學員故事 Flex] ← 既有 q5-story.js（path × 4 + Fallback Flex）
    ↓ 按 q4_story_interested
═══════ v3.2 入口（從這開始全部新建） ═══════
        ↓
[T0] webhook 即時 reply 訊息 1（textMessage + Quick Reply）+ UPDATE q5_msg1_sent_at + q5_last_pushed_at
     Quick Reply：①intro_next ②intro_maybe ③intro_question
        │
        ├─ 按 ①intro_next → reply 訊息 2 + 寫 q5_msg1_replied_at + q5_msg2_sent_at + q5_last_pushed_at
        ├─ 按 ②intro_maybe → 寫 q5_msg1_maybe_at + reply 退場語 → 永久不再自動推
        ├─ 按 ③intro_question → 寫 q5_msg1_question_at + triggerHandoff → reply 設定預期 → 永久不再推
        └─ 24h 沒按 → cron 段 5（push 訊息 2 + UPDATE）
        ↓
[T1] 訊息 2 已推（同訊息 1 邏輯，欄位換 q5_msg2_*）
        └─ 24h 沒按 → cron 段 6（push 訊息 3 + UPDATE + 升 stage=6）
        ↓
[T2] 訊息 3 已推（stage=6）
     Quick Reply：①offer_visit (URI→/apply?from=msg3) ②offer_maybe ③offer_question
        │
        ├─ 按 ①offer_visit（URI 直跳，不送 webhook event）
        │   → /apply visit handler 解析 from=msg3 寫 q5_apply_from_msg='msg3'
        │   → visit handler stage=6→7（既有 6→7 邏輯）
        ├─ 按 ②offer_maybe → 寫 q5_msg3_maybe_at → 永久不再推
        ├─ 按 ③offer_question → 寫 q5_msg3_question_at + handoff → 永久不再推
        └─ 24h 沒按 → cron 段 7（push 訊息 4 + UPDATE）
        ↓
[T3] 訊息 4 已推（stage=6）
     Quick Reply：①final_visit (URI→/apply?from=msg4) ②final_maybe ③final_question
        │
        └─ 24h 沒按 → 進 nurturing pool（v4 設計，🩹 v2 deadline 詳見 Ch.5.7）
        ↓
[/apply] visit handler stage=6→7 + 既有 q5_clicked_at 沿用
        └─ 24h 沒 submit → cron q5-visit-followup（既有，不動）
        ↓
[submit] submit_application RPC stage=8（既有，不動）
```

### 1.3 退場路徑

| 行為 | 寫欄位 | stage 變化 | 後續 |
|------|--------|-----------|------|
| 按「我再想想」 | q5_msg{N}_maybe_at | 不變 | 永久不再 cron 推；用戶下次傳意願詞 → handleV32FreeText 補充 reply + 重發當前段 Quick Reply（Ch.6.3） |
| 按「我有問題想問」 | q5_msg{N}_question_at | stage→5 | fifi 收通知；reply 設定預期「之後 Bot 不會自動回」（Ch.5.4）|
| pre-check 命中 | 走既有 | stage→5 | 既有路徑（決策 4） |
| 用戶傳貼圖/照片 | 不寫新欄位 | 不變 | reply 「方便用按鈕告訴我嗎？」+ **不重發 Quick Reply**（Ch.6.4）|
| 訊息 4 24h 沒按 | 不寫新欄位 | 不變 | 進 nurturing pool（Ch.5.7 🩹 v2）|
| **stage=3 reset 回 Q1（v2 新增 Ch.5.11）**| **清 16 欄 q5_msg* + q5_last_pushed_at + q5_apply_from_msg** | stage=1 | 用戶完整重跑 Q1-Q4 後重新進 v3.2 |

---

## 第 2 章：欄位字典（Field Contract）

### 2.1 official_line_users 新增欄位（migration_017，v2 = 16 欄）

| 欄位 | 型別 | 預設 | 由誰填 | 什麼時候填 | 消費者 |
|------|------|------|--------|-----------|--------|
| `q5_msg1_sent_at` | TIMESTAMPTZ | NULL | code（webhook q4_story_interested handler）| 即時 reply 訊息 1 | cron 段 5 |
| `q5_msg1_replied_at` | TIMESTAMPTZ | NULL | code（postback intro_next）| 用戶按「想知道 ABC」 | 量測 |
| `q5_msg1_maybe_at` | TIMESTAMPTZ | NULL | code（postback intro_maybe）| 用戶按「我再想想」 | cron 段 5 排除 |
| `q5_msg1_question_at` | TIMESTAMPTZ | NULL | code（postback intro_question）| 用戶按「我有問題想問」 + triggerHandoff | cron 段 5 排除 |
| `q5_msg2_sent_at` | TIMESTAMPTZ | NULL | code（postback intro_next 即時 / cron 段 5）| 推訊息 2 | cron 段 6 |
| `q5_msg2_replied_at` | TIMESTAMPTZ | NULL | code（postback method_next）| 用戶按「想看 12 週」 | 量測 |
| `q5_msg2_maybe_at` | TIMESTAMPTZ | NULL | code（postback method_maybe）| 同上 | cron 段 6 排除 |
| `q5_msg2_question_at` | TIMESTAMPTZ | NULL | code（postback method_question）| 同上 + triggerHandoff | cron 段 6 排除 |
| `q5_msg3_sent_at` | TIMESTAMPTZ | NULL | code（postback method_next 即時 / cron 段 6）| 推訊息 3 + 升 stage=6 | cron 段 7 |
| `q5_msg3_maybe_at` | TIMESTAMPTZ | NULL | code（postback offer_maybe）| 同上 | cron 段 7 排除 |
| `q5_msg3_question_at` | TIMESTAMPTZ | NULL | code（postback offer_question）| 同上 + triggerHandoff | cron 段 7 排除 |
| `q5_msg4_sent_at` | TIMESTAMPTZ | NULL | code（postback offer_next 即時 / cron 段 7）| 推訊息 4 | nurturing pool（v4） |
| `q5_msg4_maybe_at` | TIMESTAMPTZ | NULL | code（postback final_maybe）| 同上 | nurturing pool（v4） |
| `q5_msg4_question_at` | TIMESTAMPTZ | NULL | code（postback final_question）| 同上 + triggerHandoff | nurturing pool（v4） |
| `q5_last_pushed_at` | TIMESTAMPTZ | NULL | code（任何 q5_msg{N}_sent_at 寫入時同步寫）| 全域節流 | cron 段 5/6/7 節流 |
| **`q5_apply_from_msg`（v2 新增）** | **TEXT** | **NULL** | **code（apply/visit handler 解析 URL `?from=` query）** | **用戶按 offer_visit/final_visit 點 URI** | **量測訊息 3 vs 訊息 4 點擊歸因** |

> **v2 拿掉 q5_msg3_clicked_at**（v1 有）：URI button 不送 webhook click event（LINE 官方），永遠 NULL = 死欄位。
> **v2 加 q5_apply_from_msg**：取代 q5_msg3_clicked_at 角色，用 URL query 解析寫入。

### 2.2 既有欄位的新使用模式（v2 補解釋）

| 欄位 | 既有用途 | v3.2 新增 |
|------|---------|----------|
| `path_stage` | 0-8 | 訊息 3 推送時升 stage=6（決策 6）|
| `q5_clicked_at` | /apply 首次點擊 | 不動 |
| `q5_visit_followup_sent_at` | /apply 24h 沒 submit | 不動 |
| `q5_sent_at` | Q5 軟邀請推送 race guard | **v3.2 訊息 3 升 stage=6 但不寫 q5_sent_at**（見下方）|
| `handoff_triggered_at` / `handoff_reason` | Handoff 紀錄 | 4 段 question 共用 |

#### v3.2 訊息 3 升 stage=6 但不寫 q5_sent_at — 為什麼（v2 必修 L 補）

**問題**：既有 cron 段 3 reset 條件「stage=6 + q5_sent_at < NOW()-48h」是 Q5 軟邀請軌的 reset 機制。v3.2 訊息 3 推送也升 stage=6，會不會被誤抓？

**證明**：
- v3.2 訊息 3 推送時：`UPDATE ... SET q5_msg3_sent_at = NOW(), path_stage = 6 WHERE q5_msg3_sent_at IS NULL` — **不寫 q5_sent_at**
- 此時 user.q5_sent_at = NULL（cron 段 1 SQL 已加 `AND q5_msg1_sent_at IS NULL` 排除新軌）
- cron 段 3 SQL：`stage=6 AND q5_sent_at < NOW() - INTERVAL '48 hours'`
- PostgreSQL 三值邏輯：`NULL < anything = NULL` → WHERE「不滿足」 → **不會被誤 reset**

**結論**：v3.2 訊息 3 升 stage=6 + 不寫 q5_sent_at = 跟既有 reset 段互不干擾。下個 session 不要把 q5_msg3_sent_at 跟 q5_sent_at 合併（這是 Pattern 1 搬家幻覺陷阱）。

### 2.3 handoff.js reasonZh 字典補強

| reason | 中文化 | 觸發位置 |
|--------|-------|---------|
| `q4_story_interested` | （**移除 handoff**：v3.2 改走訊息 1）| — |
| `q4_story_question` | **學員故事 Flex「有問題想問」**（v3 補中文化）| 既有，保留 |
| `q4_story_maybe` | **學員故事 Flex「我再想想」**（v3 補中文化，但**沒 handoff**所以不會用到 reasonZh）| 既有，保留 |
| `q5_msg1_question` | 訊息 1（承接故事）「我有問題想問」 | postback intro_question |
| `q5_msg2_question` | 訊息 2（介紹 ABC）「我有問題想問」 | postback method_question |
| `q5_msg3_question` | 訊息 3（課程+早鳥）「我有問題想問」 | postback offer_question |
| `q5_msg4_question` | 訊息 4（最後提醒）「我有問題想問」 | postback final_question |

### 2.4 lib/users.js ALLOWED_KEYS（v2 描述精確化）

**決策 2 走 top-level column**，ALLOWED_KEYS **不需要動**。

**精確描述**：lib/users.js ALLOWED_KEYS（L168-184）只 gate `ai_tags` JSONB key（套用在 mergeByOp L197），**不 gate top-level column UPDATE**。既有寫 top-level 方式（q5-state.js / apply/visit / cron）全部直接打 supabase client `.update({...})`，沒有 column whitelist gate。

**因此**：16 個新欄位 → 不需要動 ALLOWED_KEYS。但**也沒有 whitelist 防錯機制** → Ch.10.4 規定 helper 集中（pushMsgN + markMsgN + clearV32Tracking）= 唯一防錯線。

### 2.5 lib/q5-msg-state.js（新建）helper 清單

```
pushMsg1(userId, replyToken, path) → reply 訊息 1 Flex + UPDATE q5_msg1_sent_at + q5_last_pushed_at
pushMsg2(userId, replyTokenOrNull) → reply or push 訊息 2 + UPDATE q5_msg2_sent_at + q5_last_pushed_at
pushMsg3(userId, replyTokenOrNull) → reply or push 訊息 3 + UPDATE q5_msg3_sent_at + q5_last_pushed_at + path_stage=6
pushMsg4(userId) → push 訊息 4（cron only）+ UPDATE q5_msg4_sent_at + q5_last_pushed_at
markMsgN(userId, n, action) → 寫 q5_msg{n}_{replied|maybe|question}_at（不寫 sent_at）
clearV32Tracking(userId) → 清 16 個 q5_msg*_at + q5_last_pushed_at + q5_apply_from_msg + handoff_triggered_at + handoff_reason（stage=3 reset 用，v3 加 handoff 欄位解死鎖 Ch.5.11）
detectWillingnessKeywords(text) → 偵測意願詞 + negation（Ch.6.3 用，v3 加 negation pre-filter）
buildQuickReplyForStage(n) → 產出當前段（msg1/2/3/4）的 Quick Reply object（v3 必修 重要 4 新增，handleV32FreeText 重發按鈕用）
```

**race guard**：
```sql
UPDATE official_line_users
SET q5_msg{N}_sent_at = NOW(), q5_last_pushed_at = NOW()
WHERE line_user_id = $1 AND q5_msg{N}_sent_at IS NULL
RETURNING q5_msg{N}_sent_at;
```
RETURNING NULL = race lost，pushHelper return false，呼叫方 swallow（不重試、不 reply 用戶）。

### 2.6 ~~lib/q5-msg-flex.js（新建）— buildMsg1Flex helper~~（v3.3 廢棄）

**v3.3 一休拍板**：訊息 1 改 textMessage（剛從學員故事 Flex 點過來，同張圖再放一次 = 重複）→ buildMsg1Flex helper 不需要寫，整個 lib/q5-msg-flex.js 不必新建。

訊息 1 用既有 `textMessage` + 手工拼 quickReply object（line.js 既有支援），跟訊息 2/3/4 同 pattern。

訊息 2/3/4 是純文字 + Quick Reply（postback 或 URI），用既有 `textMessage` + 手工拼 quickReply object（line.js 已支援）。

---

## 第 3 章：AI 輸出契約

**N/A** — v3.2 不用 AI 分類。但保留**既有 Q5 classifier 影響**：v3.2 新軌會繞過 Q5 classifier（決策 1，stage=4 dispatcher priority 分支「q5_msg1_sent_at NOT NULL → 不走 classifier」）。Q5 classifier 仍服務未進新軌的 stage=4 用戶。

---

## 第 4 章：Placeholder / 變數規則 + Path 個人化規則

**Placeholder**：N/A — 4 個訊息範本是固定文字，無變數。

**Path 服務範圍**（v3 必修 致命 2 補）：

v3.2 自動推進**只服務 4 個 path 用戶**：healthCheck / rebound / postpartum / eatOut。

**path=other / path=NULL 走原 fallback**：
- q4_continue handler（webhook L292-345 既有）對 path=other/null 用戶**不推學員故事 Flex**
- 這類用戶會走 Phase 3.3 bridging（triggerHandoff q4_followup_before_q5_wire）或既有 Q5 軟邀請軌
- v3.2 漏斗**完全不接觸這類用戶**
- 量測 baseline 必須只看 4 path 用戶（Ch.11.6 baseline 計算須 group by path）

**Path 個人化規則**（v2 必修 K 補）：

| 段 | path 個人化？ | 範本數量 | 理由 |
|---|--------------|---------|------|
| 訊息 1 | ✅ 個人化 | 4 個 path 版（**沒 Fallback**，因為 v3.2 不服務 path=other/null）| 承接學員故事必須對齊 path |
| 訊息 2 | ❌ **path 中立** | 1 個版本 | 純介紹 ABC 三框架，**核心方法不分 path** |
| 訊息 3 | ❌ **path 中立** | 1 個版本 | 12 週課程 + 三段價，**方案不分 path** |
| 訊息 4 | ❌ **path 中立** | 1 個版本 | 最後提醒 + 早鳥剩餘，**催促訊息不分 path** |

> **v3 修正**：v2 寫「訊息 1 = 4 個 path 版 + 1 Fallback」其實是錯的 — q4_story_interested 永遠不會被 path=other 用戶觸發（既有 Phase 4.6 對 path=other 不推學員故事 Flex），Fallback 永遠不會 reach。但 v3.2 的 Fallback 範本可保留作為 defensive design（萬一未來 path=other 也推學員故事 Flex 時自動接住）。

**為什麼 path 中立**：
1. cron 推送時 path 可能已不準（用戶 stage=3 reset 後 path 重設）
2. 統一管理（v1 寫 1 版 + v2 觀察 path 個別轉換率再決定是否分版）
3. 訊息 2/3/4 內容（方法 + 方案）跟 path 弱相關 — 三框架對所有 path 都成立

**例外**：訊息 1 的承接故事必須 path 個人化（無法用 1 版承接 4 種故事）。

---

## 第 5 章：觸發規則表（State Transition Table）

### 5.0 入口脈絡（v2 必修 A 補）

```
Q4 AI 個人化回饋（既有 Phase 3.2c）
  ↓ 看完 reply
Q4 Quick Reply 三按鈕（既有）：
  ① 想聽聽 (q4_continue) ② 再考慮看看 (q4_maybe) ③ 不適合 (q4_decline)
  ↓ 按 q4_continue
[既有 Phase 4.6] webhook q4_continue handler 推「學員故事 Flex」（lib/q5-story.js buildStoryFlexMessage，path × 4 + Fallback Flex）
  ↓ 學員故事 Flex 三按鈕（既有）：
  ① 想了解 ABC 在做什麼 (q4_story_interested)
  ② 有問題想問 (q4_story_question)
  ③ 我再想想 (q4_story_maybe)
  ↓ 按 q4_story_interested
═══════ v3.2 入口（5.1 改寫從這開始） ═══════
```

**重要**：v3.2 是**接在學員故事 Flex 之後**的設計，不是接在 Q4 AI 之後。改寫 q4_story_interested handler 必須保留**進入點是學員故事 Flex 的 button**（不要動 q4_continue handler 推學員故事 Flex 的邏輯，那是 Phase 4.6 既有不動）。

**Path 限制（v3 必修 致命 2）**：
- q4_continue handler（webhook L292-345）對 path=other/null **不推學員故事 Flex** → 走 Phase 3.3 bridging 或 Q5 軟邀請軌
- **v3.2 自動推進只服務 4 path 用戶**：healthCheck / rebound / postpartum / eatOut
- path=other/null 用戶不會 reach q4_story_interested → 永遠不會進 v3.2 訊息 1 → 走原 fallback handoff
- 量測時 v3.2 漏斗指標**只統計 4 path 用戶**（Ch.11.6）

### 5.1 觸發點：q4_story_interested postback（v2 改寫）

```
[現況] webhook route.js L355-366
  → triggerHandoff(userId, 'q4_story_interested') + reply「請 fifi 跟你聊聊」
[v3.2 改寫]
  ↓ 觸發：postback action='q4_story_interested'
  ↓ 條件：用戶剛看完學員故事 Flex（任何 path_stage 都允許進入）
  ↓ 動作：
    - **不 triggerHandoff**（決策 3）
    - 讀 user.path → 選對應訊息 1 範本（healthCheck/rebound/postpartum/eatOut，Fallback 為 defensive 保留但不應 reach）
    - replyMessage(replyToken, [textMessage(getMsg1Body(user.path)), 帶 Quick Reply]) — 用 textMessage + quickReply object（既有 line.js pattern），不用 buildMsg1Flex
    - **conditional UPDATE**：q5_msg1_sent_at IS NULL 才寫；race lost 不重試
    - 同次 UPDATE 寫 q5_last_pushed_at
  ↓ 下一狀態：path_stage 不變，q5_msg1_sent_at NOT NULL
```

**reply token invariant（v3 必修 重要 1 補）**：
- LINE reply token 同一個 webhook event **只能用一次**且 1 分鐘內
- q4_story_interested handler 的 reply token **由本 handler 獨佔**，pre-check（matchPoliteEnd / matchGlobalHandoff）對 postback 事件**不應該執行**（既有 webhook L117-155 pre-check 是針對 message event，不對 postback event）
- 若未來在 postback 處理流加 pre-check（例如 postback data 含關鍵字），必須**先 reply 訊息 1 + UPDATE 後**才能跑 pre-check（不能搶 token）
- Phase 2B 實作時必須 grep 所有「reply token」用法確認無衝突

### 5.2 觸發點：postback intro_next（訊息 1「想知道 ABC 怎麼運作」）（v3 必修 重要 2 改順序）

```
  ↓ 條件：q5_msg1_sent_at NOT NULL & q5_msg1_replied_at IS NULL & q5_msg2_sent_at IS NULL
  ↓ 動作（v3 改順序，先成功 reply 才寫 sent_at 防 partial fail）：
    1. conditional UPDATE 寫 q5_msg1_replied_at = NOW()（race guard，已寫 → race lost return false）
    2. replyMessage 推訊息 2 + Quick Reply（成功才繼續，LINE API 失敗則 throw）
    3. conditional UPDATE 寫 q5_msg2_sent_at + q5_last_pushed_at（race guard）
  ↓ 下一狀態：q5_msg2_sent_at NOT NULL
```

**v3 必修 重要 2 補**：v2 的問題是「步驟 1 寫 replied_at 成功後，步驟 2 LINE 503 失敗 → cron 段 5 SQL 看 replied_at NOT NULL 就排除這用戶 → 訊息 2 永遠不推」。

**修法**：cron 段 5 SQL 主軸**改成 `q5_msg2_sent_at IS NULL`**（不看 replied_at），讓 partial fail 用戶 24h 後仍被 cron 撈起來推訊息 2：

```sql
-- v3 改後 cron 段 5 SQL（主軸改 q5_msg2_sent_at IS NULL，不再看 replied_at）
SELECT line_user_id, path
FROM official_line_users
WHERE path_stage = 4
  AND handoff_triggered_at IS NULL
  AND q5_msg1_sent_at IS NOT NULL
  AND q5_msg1_sent_at < NOW() - INTERVAL '24 hours'
  AND q5_msg1_maybe_at IS NULL
  AND q5_msg1_question_at IS NULL
  AND q5_msg2_sent_at IS NULL  -- ← v3 主軸：不管 replied_at 寫了沒，只要 msg2 沒推就再推
  AND (q5_last_pushed_at IS NULL OR q5_last_pushed_at < NOW() - INTERVAL '23 hours')
  AND is_blocked = false
LIMIT 50;
```

**取捨**：partial fail 用戶 24h 後會收到 cron 推的訊息 2（可能跟 webhook 推訊息 2 race lost 那次有點延遲，但不會永遠卡死）。

### 5.3 觸發點：postback intro_maybe（「我再想想」）

```
  ↓ 條件：q5_msg1_sent_at NOT NULL & q5_msg1_maybe_at IS NULL
  ↓ 動作：
    - conditional UPDATE 寫 q5_msg1_maybe_at = NOW()
    - replyMessage 「好，沒問題。如果之後想了解再來找我就好，不打擾你。」
    - **不 handoff、不推訊息 2**（永久不再 cron 推）
  ↓ 下一狀態：q5_msg1_maybe_at NOT NULL
```

### 5.4 觸發點：postback intro_question（「我有問題想問」）（v2 必修 B 改 reply 文案）

```
  ↓ 條件：q5_msg1_sent_at NOT NULL & q5_msg1_question_at IS NULL
  ↓ 動作：
    - conditional UPDATE 寫 q5_msg1_question_at = NOW()
    - triggerHandoff(userId, 'q5_msg1_question') → stage=5 + notify fifi/一休
    - replyMessage（v2 改寫設定預期）：
      「好，你的問題請說，我請 fifi 助教看到後跟你聊。
      上班時間會陸續回，不會讓你等太久。
      ※ 之後 Bot 不會自動回，由 fifi 接手。」
  ↓ 下一狀態：path_stage=5，q5_msg1_question_at NOT NULL
  ↓ 永久不再 cron 推
```

**為何加「Bot 不會自動回」**：webhook stage=5 既有路徑直接靜默（route.js L868），用戶傳字 Bot 不回。沒明說 = 用戶以為 Bot 壞了。明說 = 用戶心理預期對 = 等 fifi 接手。

### 5.5 method_next / method_maybe / method_question

同 5.2 / 5.3 / 5.4 模式，欄位換 q5_msg2_*，下一段 sent 換 q5_msg3_sent_at（**push helper 內升 stage=6**）。method_question reply 文案同 5.4。

### 5.6 offer_visit / offer_maybe / offer_question（v3 必修 致命 1 補完整 5 處配套）

```
[offer_visit] type=URI（不送 webhook click event，LINE 官方確認）
  - URI = `https://official-yihugh-line-bot.vercel.app/apply?from=msg3&<既有 HMAC sig query>`
  - LINE 官方文件：URI button 不送 webhook event，純打開 URL
  - 用戶點進 /apply landing → landing 解析 ?from + HMAC sig → POST /api/apply/visit body 帶 from → visit handler 寫 q5_apply_from_msg='msg3'
  - visit handler stage=6→7（既有 6→7 邏輯）

[offer_maybe] 同 5.3，欄位 q5_msg3_maybe_at
[offer_question] 同 5.4，欄位 q5_msg3_question_at + handoff reason='q5_msg3_question'
```

**v3 必修 致命 1 完整 5 處配套**（v2 只寫 URI 加 query 但沒寫 landing/visit 怎麼接 → 永遠寫不到 q5_apply_from_msg）：

| 步驟 | 改檔案 | 改什麼 |
|------|-------|-------|
| 1 | `lib/q5-apply-url.js` buildQ5ApplyUrl | URL builder 加 from 參數 → `?from=msg3&...HMAC params...`（**from 不進 HMAC canonical payload，不簽名**）|
| 2 | `app/apply/page.js` extractHmacParams（L488-498）| 加讀 from query（optional，不在 HMAC_KEYS）→ 存 hmacParams.from |
| 3 | `app/apply/page.js` fetch /api/apply/visit body（L568-573）| body 加 `from: hmacParams.from`（不參與 HMAC verify）|
| 4 | `app/api/apply/visit/route.js` BODY_KEYS（L18）| 加 from（optional）→ shape check：from in `['msg3', 'msg4', null]` |
| 5 | `app/api/apply/visit/route.js` UPDATE block | 加 `q5_apply_from_msg = COALESCE(existing, p_from)` 寫入 |

**安全分析**（為何 from 不進 HMAC canonical 安全）：
- from 是**量測 metadata**，不影響 auth / userid 鎖定
- 攻擊者篡改 from（例：把 msg3 改 msg4）→ 只是污染歸因 metric，不會獲得任何權限
- HMAC sig 已綁 userid + ts + sig + kv + source + trigger（5 欄 canonical），from 加進去會破壞既有 sig 結構（既有 URL 沒 from）
- 結論：from 不進 sig 是**有意設計**，不是漏洞

**Phase 0 必須補**：訊息範本 v3.3 的 URI 範例必須帶 `?from=msg3` / `?from=msg4`（範本撰寫者不能忘）。
**Phase 2A 必須補**：Vercel env 不需動（只改 code）。
**Phase 2B 必須補**：步驟 1-5 全做完才能 deploy（5 處不同步 → from 永遠寫不到）。
**Phase 2D 必須驗**：實測「按 offer_visit → /apply landing → submit」，DB 看 q5_apply_from_msg='msg3'。

### 5.7 final_visit / final_maybe / final_question + nurturing pool（v2 必修 N）

```
[final_visit] type=URI = `/apply?from=msg4` + HMAC sig
[final_maybe] 同 5.3，欄位 q5_msg4_maybe_at
[final_question] 同 5.4，欄位 q5_msg4_question_at + handoff reason='q5_msg4_question'

訊息 4 24h 沒按按鈕：
  → 進 nurturing pool（🩹 v2 deadline：2026-06-30 前實作）
  → v3.2 v1 階段「進 nurturing pool」= q5_msg4_sent_at < NOW()-24h + 任一 maybe/question/visit 都沒寫
  → admin 後台需加「nurturing 候選清單」視覺化（v2 工作，Ch.11.1）
  → 推播策略待 v4 設計（7d/14d/28d/60d 教育推播 + 說明會通知）
  → ⚠️ v2 之前**用戶會被遺留**，必須在 admin Tab 加「v3.2 終態用戶清單」讓 fifi 手動看
```

### 5.8 stage 升 6 的時機（決策 6 配套）

訊息 3/4 的 URI 按鈕直接跳 /apply，webhook 收不到事件。解法（方案 A）：訊息 3 推送時 pushMsg3 helper 內順便升 stage→6。

```sql
UPDATE official_line_users
SET q5_msg3_sent_at = NOW(),
    q5_last_pushed_at = NOW(),
    path_stage = 6,
    path_stage_updated_at = NOW()
WHERE line_user_id = $1
  AND q5_msg3_sent_at IS NULL
  AND path_stage <= 4  -- 避免覆蓋 stage=5（handoff）/7（已點 apply）
RETURNING q5_msg3_sent_at;
```

訊息 1/2 推送**不升 stage**（保持 path_stage=4）。訊息 3/4 推送時升 stage=6。

### 5.9 cron q5-maintenance 7 段（v2 必修 D + O 改）

| 段 | 名稱 | SQL 條件 | 動作 |
|---|------|---------|------|
| 1 | active（既有，改）| stage=4 + q5_sent_at IS NULL + ...（既有條件）+ **q5_msg1_sent_at IS NULL**（決策 1） | performQ5Transition(active) + pushQ5SoftInvite |
| 2 | visit-followup（既有）| stage=7 + q5_visit_followup_sent_at IS NULL + q5_clicked_at < NOW()-24h | push q5_visit_followup_text |
| 3 | reset（既有）| stage=6 + q5_sent_at < NOW()-48h | stage 6→4 + 清 q5_sent_at（v3.2 用戶 q5_sent_at IS NULL 不命中，Ch.2.2 已證明）|
| 4 | notify retry（既有）| applications.notify_status IN (pending, failed) | push 給一休+婉馨 |
| **5** | **msg2 24h 推進（新）** | **path_stage = 4** + **handoff_triggered_at IS NULL**（必修 D）+ q5_msg1_sent_at < NOW()-24h + q5_msg1_replied_at/maybe_at/question_at IS NULL + q5_msg2_sent_at IS NULL + (q5_last_pushed_at IS NULL OR < NOW()-23h) + is_blocked=false | pushMsg2 push（無 replyToken）+ UPDATE 兩欄 |
| **6** | **msg3 24h 推進（新）** | 同段 5 換 msg2/msg3 | pushMsg3 + UPDATE 三欄（含 path_stage=6）|
| **7** | **msg4 24h 推進（新）** | **path_stage IN (4, 6)** + handoff_triggered_at IS NULL + 同段 5 換 msg3/msg4 | pushMsg4 + UPDATE 兩欄 |

**段 5 SQL 範例**（v2）：
```sql
SELECT line_user_id, path
FROM official_line_users
WHERE path_stage = 4
  AND handoff_triggered_at IS NULL  -- v2 必修 D
  AND q5_msg1_sent_at IS NOT NULL
  AND q5_msg1_sent_at < NOW() - INTERVAL '24 hours'
  AND q5_msg1_replied_at IS NULL
  AND q5_msg1_maybe_at IS NULL
  AND q5_msg1_question_at IS NULL
  AND q5_msg2_sent_at IS NULL
  AND (q5_last_pushed_at IS NULL OR q5_last_pushed_at < NOW() - INTERVAL '23 hours')
  AND is_blocked = false
LIMIT 50;  -- v2 必修 O：100 → 50 保守
```

**maxDuration**：60s。**concurrency**：10。**LIMIT**：段 5/6/7 各 **50**（v2 必修 O 從 100 降）。
**Phase 2C 必加**：每段 `console.time('q5-cron-section-N')` + `console.timeEnd` log，觀察 P95 < 50s 才安全（>= 45s 拆路由）。

### 5.10 stage=4/6 dispatcher（v2 必修 G 改寫）

**stage=4 dispatcher**（priority 1 新增）：
```javascript
if (stage === 4) {
  // priority 1: v3.2 新軌中 → 不走 Q5 classifier
  if (user.q5_msg1_sent_at !== null) {
    return handleV32FreeText(event, user);  // Ch.6.3
  }
  // priority 2: 既有 Q5 classifier（不變）
}
```

**stage=6 dispatcher**（v2 改寫）：

> **重要更正**：v1 假設「priority 2 既有 stage=6 邏輯」存在，**錯了**。webhook route.js L1294 `// stage >= 5 → 靜默` = stage=6 沒有專屬 dispatcher。

```javascript
if (stage === 6) {
  // priority 1: v3.2 新軌（訊息 3 或 4 已推） → handleV32FreeText
  if (user.q5_msg3_sent_at !== null || user.q5_msg4_sent_at !== null) {
    return handleV32FreeText(event, user);
  }
  // priority 2: 既有 Q5 軟邀請軌（q5_sent_at NOT NULL 但 q5_msg3_sent_at IS NULL）→ 靜默（既有預設行為，不變）
  return false;
}
```

**插入位置**：webhook route.js L1294 之前。既有 stage=4 dispatcher 之後。

### 5.11 stage=3 reset 路徑必須清 v3.2 tracking 欄位（v2 必修 C 新增）

**問題**：webhook route.js L1313-1334 stage=3 reset 回 Q1 時只清 path/q3/q4 欄位，**不清 q5_msg{1-4}_*_at**。

**情境**：用戶 A 走完 Q4 → 進 v3.2 按 intro_next（q5_msg1/2_sent_at 寫入）→ 中途想重來打體重數字 → stage=3 reset 回 stage=1 → 重跑 Q1-Q4 → stage=4 → priority 1 q5_msg1_sent_at NOT NULL → 走 handleV32FreeText 不走 Q5 classifier → **行為斷裂**。

**修法**：reset 路徑必須同時呼叫 `clearV32Tracking(userId)`（lib/q5-msg-state.js helper），清以下欄位：
- q5_msg1_sent_at / replied_at / maybe_at / question_at
- q5_msg2_sent_at / replied_at / maybe_at / question_at
- q5_msg3_sent_at / maybe_at / question_at
- q5_msg4_sent_at / maybe_at / question_at
- q5_last_pushed_at
- q5_apply_from_msg
- **handoff_triggered_at**（v3 必修 致命 3 加，解死鎖）
- **handoff_reason**（v3 必修 致命 3 加）

**v3 必修 致命 3 補（死鎖修法）**：v2 只清 16 個 q5_msg* 欄位，**不清 handoff_triggered_at**。死鎖情境：
- 用戶按 intro_question → handoff stage=5（handoff_triggered_at NOT NULL）→ fifi 處理完
- 用戶想重來打體重 → reset 清 16 欄但 handoff_triggered_at 仍 NOT NULL
- 重跑 Q1-Q4 重按 q4_story_interested → 寫 q5_msg1_sent_at → cron 段 5 SQL `handoff_triggered_at IS NULL` **永遠排除這用戶** → 訊息 2/3/4 永遠不推 = 死鎖

**v3 修法**：reset 同時清 handoff_triggered_at + handoff_reason → 用戶重跑後 cron 能正常撈到。

用戶完整重跑 Q1-Q4 後重新進 v3.2 = 行為一致。

**實作**：webhook L1313-1334 reset block 內加 `await clearV32Tracking(userId)`（async）+ Phase 2B 任務清單必含此項。

**邊界**：reset 不清 path_stage（reset 本身就是 set path_stage=1）+ 不清 ai_tags（既有清 q4_classified_at 已涵蓋）。

---

## 第 6 章：Handoff / Fallback 規則

### 6.1 v3.2 觸發 handoff 的入口

| 入口 | reason | 寫欄位 |
|------|--------|-------|
| 用戶按 intro_question | q5_msg1_question | q5_msg1_question_at + handoff_triggered_at + path_stage=5 |
| 用戶按 method_question | q5_msg2_question | q5_msg2_question_at + ... |
| 用戶按 offer_question | q5_msg3_question | q5_msg3_question_at + ... |
| 用戶按 final_question | q5_msg4_question | q5_msg4_question_at + ... |
| 既有 pre-check 命中（決策 4）| 既有 reason | 既有寫法不變 |

### 6.2 handoff 通知格式（v2 必修 M 補 v3.2 進度）

handoff.js notifyHandoff 既有格式不動，**新增 v3.2 用戶 push 文字**：

```javascript
const v32StageMap = {
  q5_msg1_question: '訊息 1（承接故事）',
  q5_msg2_question: '訊息 2（介紹 ABC）',
  q5_msg3_question: '訊息 3（課程+早鳥）',
  q5_msg4_question: '訊息 4（最後提醒）',
};
const v32Stage = v32StageMap[reason];
const v32Line = v32Stage ? `\nv3.2 進度：${v32Stage} 後問問題` : '';
```

**婉馨收到的訊息範例**：
```
🔴 高意願用戶進 Handoff
名字：王小美
路徑：產後
第 5 步
想瘦：62 → 55 公斤
代謝類型：rollerCoaster
Handoff 原因：訊息 3（課程+早鳥）後問問題
v3.2 進度：訊息 3（課程+早鳥）後問問題
痛點：抗拒不了甜食、產後肚子收不回去

→ 開對話：https://official-yihugh-line-bot.vercel.app/admin?user=U...
```

### 6.3 fallback：用戶在新軌中傳自由文字（非 postback）（v2 必修 J 改）

```
條件：q5_msg{1-4}_sent_at NOT NULL & 沒按按鈕（未寫 maybe/question/replied）& 收到 message event
動作：
  1. 先跑既有 pre-check（matchPoliteEnd / matchGlobalHandoff，決策 4）
     → 命中 → handoff 既有路徑（stage→5）
  2. 沒命中 → 偵測「意願詞」（v2 必修 J 補 + v3 必修 重要 3 加 negation pre-filter）：
     - **negation pre-filter**（v3 加，先檢查）：
       - 命中 negation regex `/不[想了解OK好]|沒[興趣意願]/` → 視為 decline，**不算意願**
       - 例：「不想再聊了」「不了解」「不 OK」「沒興趣」「沒意願」→ 直接走「沒命中」分支
       - 「謝謝不用了」「不用了」沒命中（無「不+意願詞」結構）→ 也走「沒命中」（OK）
     - **意願詞清單**（pre-filter 通過後才檢查）：「想」「了解」「OK」「好」「再聊」「報名」「價格」「課程」（regex / contains）
     - **命中意願詞** → reply「想知道更多嗎？我把選項再貼給你 →」+ **重發當前段 Quick Reply**（用 `buildQuickReplyForStage(n)` helper，Ch.2.5）
     - **沒命中（含 negation pre-filter 命中）** → reply「我有看到你的訊息～方便用下面的按鈕告訴我嗎？」+ **不重發 Quick Reply**
  3. **不寫任何新欄位**（cron 仍會繼續 24h 推進）

判斷「當前段」：q5_msg{N}_sent_at NOT NULL 中 N 最大且 q5_msg{N}_replied_at IS NULL 那一段
```

**為何用「意願詞偵測」而不是 reply 一律重發**：
- 用戶按 maybe 後傳「謝謝」「OK」沒重發 = 不打擾
- 用戶按 maybe 後傳「想了解了」是真實高意願 = 重發 Quick Reply 救援
- 防 Pattern 8（痛點誇大假設用戶會自己往上找按鈕）

### 6.4 fallback：用戶傳貼圖/照片/非文字

reply「我有看到你的訊息～方便用文字或下面的按鈕告訴我嗎？」+ **不重發 Quick Reply**（貼圖通常不是高意願）。

### 6.5 fallback：用戶按過 question 後 stage=5 傳字（v2 必修 B）

webhook L868 既有 stage=5 直接 return 靜默 — **v3.2 不修這個**（fifi 人工接管 = 不該由 Bot 自動回）。Ch.5.4 reply 文案明說「之後 Bot 不會自動回」= 用戶心理預期對。

---

## 第 7 章：多選 / 歧義處理

### 7.1 用戶連續快速按多個 postback（race condition）

conditional UPDATE 第一個寫成功，第二個 race lost → reply「我已經收到你的選擇了～」

### 7.2 用戶按過「我再想想」後又主動回字（v2 必修 J 強化）

走 stage=4 dispatcher priority 1 → handleV32FreeText：
1. pre-check 命中「想報名」「家人也要」 → handoff fifi
2. 沒命中 → 意願詞偵測（Ch.6.3）：
   - 命中 → reply + 重發當前段 Quick Reply（救回對話）
   - 沒命中 → reply 不重發

**v3.2 v1 不主動「丟回自動軌」**（cron 不重新推訊息 2/3/4，因 q5_msg1_maybe_at NOT NULL 已永久排除）。意願詞救援只是「重發 Quick Reply」讓用戶能再選一次，**用戶按 intro_next 才會繼續推進**。

### 7.3 用戶在訊息 1/2 推完後直接點 /apply（沒按按鈕）

visit handler 不升 stage（既有只認 stage=6→7）→ submit 後 RPC 仍走 stage=8 → 中間態 stage=7 漏 = q5_clicked_at 寫不到。

**取捨**：v3.2 v1 **不補修**（罕見）。**監控**：上線後若 SQL 看到「submit + q5_clicked_at IS NULL」 ≥ 5%，回頭 visit handler 加複合條件升 stage=7。

### 7.4 用戶按過 question（handoff）後 fifi 處理完，用戶又主動回字

stage=5 webhook L868 直接 return 靜默 = **Bot 不回**。Ch.5.4 reply 文案已設定預期。**v3.2 不主動丟回自動軌**。

### 7.5 用戶在新軌中被 unfollow / blocked

SQL 條件 `AND is_blocked = false` 排除。push 失敗（LINE 403）→ markBlocked 既有邏輯。q5_msg{N}_sent_at 在 push 前已 commit → race-safe。

### 7.6 訊息 4 推完用戶按按鈕但 click /apply 失敗

既有 stage=7 路徑兜接（visit handler 已升 7）→ /apply 重試或 fifi 接手。v3.2 不另開路徑。

### 7.7 用戶 stage=3 reset 回 Q1 期間（v2 必修 C）

reset 同時 `clearV32Tracking(userId)` 清 16 欄位 + q5_apply_from_msg → 用戶完整重跑 Q1-Q4 → 重新進學員故事 Flex → 重新按 q4_story_interested → 重新走 v3.2 訊息 1。

**邊界**：reset 之前用戶的 path 可能變了（原 healthCheck → 重跑後改 rebound）→ 訊息 1 範本選新 path 對應的版本。

---

## 第 8 章：閾值 / 常數表（v2 必修 I 改 HMAC）

| 常數 | 值 | 存哪裡 | 後台可改 |
|------|---|--------|---------|
| 訊息 1→2 cron 推進 timeout | `INTERVAL '24 hours'` | code 常數 lib/q5-msg-state.js | 不可改 |
| 訊息 2→3 cron 推進 timeout | `INTERVAL '24 hours'` | 同上 | 不可改 |
| 訊息 3→4 cron 推進 timeout | `INTERVAL '24 hours'` | 同上 | 不可改 |
| 全域節流（一天最多推一則）| `INTERVAL '23 hours'` | 同上 | 不可改 |
| cron schedule | `0 * * * *` | vercel.json | 不可改 |
| cron maxDuration | 60s | code 既有 | 不可改 |
| cron concurrency | 10 | code 既有 | 不可改 |
| cron 段 5/6/7 LIMIT | **50**（v2 必修 O 從 100 降）| code | 不可改 |
| **HMAC URL 過期（v2 必修 I 改）** | **`Q5_APPLY_SIG_MAX_AGE_SEC=172800`（48h）** | Vercel env var | 改 env var |
| 訊息 4 沒回 → nurturing 進入時間 | （v4 設計，🩹 v2 deadline 2026-06-30）| — | — |
| /apply visit handler stage 升級條件 | `path_stage === 6` | code 既有 | 不可改 |

**v2 改 HMAC 過期 86400 → 172800**（24h → 48h）的理由：
- 訊息 4 推完後用戶 24h 內沒按 → cron 不會再推（進 nurturing pool）
- 用戶可能 30h 後才打開 LINE 看到訊息 4 → 點 final_visit URL → 過期 → /apply/visit 回 invalid_signature 400
- 改 48h：訊息 4 推完後 24h（cron 等待）+ 24h（用戶看訊息）= 48h 內有效
- trade-off：URL 有效期變長 = HMAC 重放攻擊風險視窗變大，但 LINE userId 鎖定 + click_count 追蹤已防禦，可接受

**為何節流用 23h 而非 24h**：cron 每小時整點跑，連續兩次「24h 整」可能落同一 cron tick → 23h 緩衝。

---

## 第 9 章：寫入規則（Data Lifecycle）

### 9.1 寫入模式：append（不可 overwrite）

q5_msg{N}_*_at 14 個欄位 + q5_apply_from_msg + q5_last_pushed_at = **16 個欄位**：
- q5_msg*_at 14 個寫入 NULL → NOT NULL 後不再回寫（量測歷史紀錄不能丟）
- q5_apply_from_msg 第一次寫入後不覆蓋（同 q5_clicked_at COALESCE 模式）
- q5_last_pushed_at **可覆寫**（每次推送都更新，是節流用不是歷史紀錄）

**例外**：
- cron reset（既有段 3）會清 q5_sent_at（不影響 v3.2）
- **stage=3 reset 路徑（v2 新增 Ch.5.11）會 clearV32Tracking 清 16 欄位**（有意 reset，不是覆寫）

### 9.2 race guard：conditional UPDATE

所有 push helper 用 `WHERE q5_msg{N}_sent_at IS NULL` race guard，RETURNING 判斷成功與否。

### 9.3 freshness：v3.2 沒有 freshness 機制

q5_msg{N}_*_at 寫入後永遠保留，不重估、不過期。

### 9.4 清除條件

| 觸發 | 清哪些欄位 | helper |
|------|----------|--------|
| stage=3 reset 回 Q1（v2 必修 C）| 16 個 q5_msg*_at + q5_last_pushed_at + q5_apply_from_msg | clearV32Tracking |
| 其他 | 不清 | — |

**未來情境**（v2 backlog）：用戶 unblock + path 改變 → 是否從新 path 重跑訊息 1？v1 不解。

---

## 第 10 章：責任分工

### 10.1 v3.2 涉及 Phase

| Phase | 工作 | 不做什麼 |
|-------|------|---------|
| **Phase 2A**（schema）| migration_017 加 16 欄（含 q5_apply_from_msg）+ handoff.js reasonZh 補 4 個中文化 + Vercel env Q5_APPLY_SIG_MAX_AGE_SEC 86400→172800 | 不動 ALLOWED_KEYS（決策 2 走 top-level）|
| **Phase 2B**（webhook + lib）| (1) q4_story_interested handler 改寫（5.1）(2) lib/q5-msg-state.js 7 個 helper（含 clearV32Tracking + detectWillingnessKeywords + buildQuickReplyForStage）(3) ~~lib/q5-msg-flex.js~~（v3.3 廢棄，訊息 1 用 textMessage）(4) postback routing table 重構（附錄 A）(5) 12 個新 action handler (6) stage=4/6 dispatcher 加 priority 分支（5.10）(7) **stage=3 reset 路徑加 clearV32Tracking 呼叫**（5.11）(8) handleV32FreeText 含意願詞偵測（6.3）(9) handoff.js notifyHandoff 加 v3.2 進度文字（6.2）| 不動既有 Q5 classifier（priority 讓位給新軌）|
| **Phase 2C**（cron）| q5-maintenance 加段 5/6/7（LIMIT 50 + console.time）+ 既有段 1 SQL 加 `AND q5_msg1_sent_at IS NULL` | 不動既有段 2/3/4 |
| **Phase 2D**（apply/visit + 測試）| /apply/visit handler 解析 `?from=` query 寫 q5_apply_from_msg + URI 在訊息 3/4 範本帶 query + TEST_ALLOWLIST 跑 5-10 次完整流程 + 觀察 cron P95 timing | 不對全量用戶開放（先觀察一週）|
| **Phase 2E**（admin + nurturing 補位）| admin 加「v3.2 進度」用戶 tab 顯示 + 「nurturing 候選清單」視覺化（🩹 v2 必補，2026-06-30 前）| — |

### 10.2 Phase 0 前置（必須先存在）

| 工作 | 狀態 |
|------|------|
| 訊息 1 文案（path × 4，**textMessage**）| ✅ v3.2 範本文字已有，v3.3 改 textMessage 拿掉 image 需求 |
| **訊息 2 文案（path 中立，純介紹 ABC 三框架，不講價）**| ❌ 缺 |
| **訊息 3 文案（path 中立，offer + 三段價 + URI 跳 /apply?from=msg3）**| ❌ 缺 |
| **訊息 4 文案（path 中立，最後提醒 + URI 跳 /apply?from=msg4）**| ❌ 缺 |
| **12 個 postback button label**（每段 3 個 × 4 段，每個 ≤20 字 LINE 限制）| ❌ 缺，附錄 A 有 action 規範但 label 文字待敲 |
| **4 段 maybe reply 文案**（intro_maybe / method_maybe / offer_maybe / final_maybe）| ❌ 缺 |
| **4 段 question reply 文案**（intro/method/offer/final_question）| ❌ 缺，5.4 寫的「請說，我請 fifi…」只是 intro_question 一段 |
| **意願詞偵測 reply 文案**（Ch.6.3 命中意願詞 reply「想知道更多嗎…」 + 沒命中 reply「方便用按鈕」）| ❌ 缺 |
| **race lost reply 文案**（Ch.7.1「我已經收到你的選擇了～」）| ❌ 缺 |

**Phase 0 文案總清單**（v3 必修 重要 7 補完整）：
- ✅ 訊息 1 文字（4 path）
- ❌ 訊息 1 hero image URL（4 path 各一張，例如沛蓁 / 俐臻 / 溫溫 / 美美的形象照）
- ❌ 訊息 2 / 3 / 4 文字（共 3 段，path 中立）
- ❌ 12 button label（每段 3 個 × 4 段）
- ❌ 4 段 maybe reply
- ❌ 4 段 question reply
- ❌ 意願詞 reply（命中 + 沒命中各 1 條）
- ❌ race lost reply
- 共 **3 段文字 + 4 image URL + 12 button label + 4 maybe + 4 question reply + 2 意願詞 + 1 race lost = ~30 條文案**

**文案規則（v3 必修 重要 8 補）**：
- 過 `.claude/rules/brand-and-style.md`（一休語感）
- 不販賣焦慮、加法思維、結尾簽名「我是一休，陪你健康的瘦一輩子」（健康文章 brand）
- 5.4 reply「※ 之後 Bot 不會自動回，由 fifi 接手」**不能用「※」開頭**（太機關 / 太冷）→ 要改成自然口氣，例：「之後我先讓 fifi 接，她看到會跟你聊，我先不打擾你～」（待 yi-voice 自審）
- Phase 0 文案寫完跑一遍 yi-voice agent 自審（雙標準：①五特質 ②三標準）

**前置阻塞**：~30 條文案不齊，Phase 2A 不能動 schema。

**動 code 順序**：先**訊息範本 v3.3 文案 30 條落地（過 yi-voice 自審）** → Phase 2A → 2B → 2C → 2D → 2E。

**Phase 0 文案撰寫時間估**（一休本人）：**~2.5-3 小時**（v2 估 1.5-2h 漏算 button label / maybe reply / question reply / 意願詞 reply / race lost reply）。

### 10.3 Code 環境

- ESM `import / export`
- TypeScript 不用
- Vercel UTC + cron SQL 用 `NOW() - INTERVAL`（不 hardcode 小時）
- migration SQL 不寫 BEGIN/COMMIT（Supabase MCP 自動包）

### 10.4 helper 公用化規則（Pattern 7）

寫入 q5_msg{N}_sent_at 的 4 處（postback 即時 / cron 段 5-7 / pushMsg{N} helper / **stage=3 reset 清欄位**）必須**全部走** lib/q5-msg-state.js helper，不准 inline SQL。

**helper 集中清單（lib/q5-msg-state.js）**：
- pushMsg1 / pushMsg2 / pushMsg3 / pushMsg4
- markMsgN（寫 replied/maybe/question_at）
- clearV32Tracking（stage=3 reset 用，**v3 加清 handoff_triggered_at + handoff_reason**）
- detectWillingnessKeywords（意願詞偵測 + **v3 加 negation pre-filter**，6.3 用）
- **buildQuickReplyForStage(n)**（v3 必修 重要 4 加，handleV32FreeText 重發當前段 Quick Reply 用）

~~**helper 集中清單（lib/q5-msg-flex.js）**：buildMsg1Flex(path)~~ — **v3.3 廢棄**，訊息 1 改 textMessage 不需要新檔。

**所有訊息共用模式**：訊息 1/2/3/4 全部用 `textMessage` + 手工拼 quickReply object（line.js 既有支援）。

### 10.5 Phase 2D 測試 checklist（含必修點驗證）

- [ ] q4_story_interested → reply 訊息 1（不 handoff）
- [ ] intro_next 推訊息 2（即時 reply）
- [ ] intro_maybe 寫 q5_msg1_maybe_at
- [ ] intro_question 寫 q5_msg1_question_at + handoff + reply 含「Bot 不會自動回」
- [ ] cron 段 5 推訊息 2（24h 沒按）
- [ ] **cron 段 5 排除 handoff 用戶**（pre-check 命中升 stage=5 但 q5_msg1_question_at NULL 的測試）
- [ ] **stage=3 reset 路徑清 16 欄位**（用戶按 intro_next 後傳體重數字）
- [ ] **訊息 3 URI 帶 ?from=msg3，apply/visit 寫 q5_apply_from_msg='msg3'**
- [ ] **訊息 4 URI 帶 ?from=msg4，apply/visit 寫 q5_apply_from_msg='msg4'**
- [ ] handoff notify 訊息含「v3.2 進度：訊息 N」
- [ ] 按 maybe 後傳「想了解」reply + 重發 Quick Reply（意願詞偵測）
- [ ] 按 maybe 後傳「謝謝」reply 不重發 Quick Reply
- [ ] cron 7 段 P95 timing < 50s

---

## 第 11 章：跨系統介面

### 11.1 admin 後台

| 功能 | v3.2 影響 | 動作 |
|------|---------|------|
| 用戶 tab 篩選 | 新增 path_stage / handoff_reason 篩選 | Phase 2E（觀察期後） |
| 用戶 tab 顯示 | 新增 q5_msg{N}_sent_at 4 欄位 + q5_apply_from_msg | Phase 2E |
| **「nurturing 候選清單」視覺化（🩹 v2 必修 N）**| **q5_msg4_sent_at NOT NULL AND 全 maybe/question/visit NULL 用戶清單** | **Phase 2E（2026-06-30 前）** |
| 報名 tab | 不動 | — |

**v3.2 v1 不動 admin**：先用 SQL 撈數據看效果。但 nurturing 候選清單**必須在 v2 補位**（用戶會被遺留問題）。

### 11.2 既有 cron q5-maintenance 影響

| 段 | 既有 v.s. v3.2 | 動作 |
|---|---------------|------|
| 段 1 active | SQL 加 `AND q5_msg1_sent_at IS NULL` 排除新軌 | Phase 2C |
| 段 2/3/4 | 不變 | — |

### 11.3 既有 Q5 classifier

| 項目 | v3.2 影響 |
|------|---------|
| Q5 classifier wire | stage=4 dispatcher priority 分支讓位給新軌 |
| 既有 Q5 流程 | 對 stage=4 + q5_msg1_sent_at IS NULL 用戶仍生效 |
| 共存策略 | 新軌 + 舊軌並行 30 天，30 天後評估是否砍舊軌 |

### 11.4 abc-line-bot / coach-line-bot

**完全不影響**。

### 11.5 LINE Console webhook redelivery

conditional UPDATE race guard 已防（重複收到同 postback → race lost return）。

### 11.6 量測指標（成功標準，v2 改 #3）

跑完 v3.2 後，能從 SQL 算出：
1. **訊息 1 → 訊息 2 轉換率**：`COUNT(q5_msg1_replied_at NOT NULL) / COUNT(q5_msg1_sent_at NOT NULL)`
2. **訊息 2 → 訊息 3 轉換率**：同上換 msg2
3. **訊息 3 → /apply 點擊率（v2 改用 q5_apply_from_msg）**：`COUNT(q5_apply_from_msg = 'msg3') / COUNT(q5_msg3_sent_at NOT NULL)`
4. **訊息 4 → /apply 點擊率（v2 新增）**：`COUNT(q5_apply_from_msg = 'msg4') / COUNT(q5_msg4_sent_at NOT NULL)`
5. **/apply → submit 轉換率**：`COUNT(stage=8) / COUNT(q5_clicked_at NOT NULL)`
6. **端到端**：`COUNT(stage=8 WHERE q5_msg1_sent_at NOT NULL) / COUNT(q5_msg1_sent_at NOT NULL)`
7. **path 個別表現**：上面 6 個指標 group by path
8. **退場路徑統計**：`COUNT(q5_msg{N}_maybe_at NOT NULL)` / `COUNT(q5_msg{N}_question_at NOT NULL)`

**Definition of Done（觀察期一週後，v3 必修 重要 6 改 baseline）**：
- **端到端轉換率 > baseline 4.3%**（v3 改：v2 寫「過去 1-2%」是錯的，5/3 實測 3/69 ≈ 4.3% 是真實 baseline。1-2% 是過去全 path 用戶 baseline，v3.2 只服務 4 path 用戶不該對齊全 path 數字 → 用 4.3% 才是 fair comparison）
- 「我有問題想問」按下率 < 30%（太高表示文案沒講清楚）
- cron 段 5/6/7 P95 < 50s（>= 45s 拆路由）
- nurturing pool 待處理用戶數量列出（觸發 v2 admin 動）

**baseline 計算公式**（v3 補）：
```sql
-- v3.2 上線前的 baseline（5/3 實測）
SELECT
  COUNT(*) FILTER (WHERE path IN ('healthCheck','rebound','postpartum','eatOut')) AS denom,
  COUNT(*) FILTER (WHERE path IN ('healthCheck','rebound','postpartum','eatOut') AND path_stage = 8) AS numerator
FROM official_line_users
WHERE handoff_triggered_at >= '2026-04-01' AND handoff_triggered_at < '2026-05-01';
-- baseline ≈ 3/69 = 4.3%
```

v3.2 上線後一週重跑同 SQL 看 numerator/denom 是否提升。

### 11.7 v2.x 觀察期 backlog（5 個尚可洞）

- postback redelivery race lost reply 改用 push（v1 用 reply token，redeliver 時 token 已用會被 LINE 擋，但「我已經收到」訊息可能對 redeliver 場景產生副作用）
- cron 段 1 也加 q5_last_pushed_at 節流（避免 Q5 軟邀請 + 訊息 1 一天兩推）
- concurrency=10 多段共撈（Phase 2D 觀察）
- Ch.2.4 描述精確化（已在 v2 補一句）
- **cron 段 5/6/7 partial index**（v3 必修 review 加）：當 official_line_users 用戶量到 500+（目前 862，持續成長）時，cron 段 5/6/7 SQL 會 full scan，建議加 partial index：
  ```sql
  CREATE INDEX IF NOT EXISTS idx_q5_msg_pending_msg2
    ON official_line_users (q5_msg1_sent_at)
    WHERE path_stage=4 AND q5_msg1_sent_at IS NOT NULL AND q5_msg2_sent_at IS NULL AND handoff_triggered_at IS NULL;
  -- 同模式 idx_q5_msg_pending_msg3 / idx_q5_msg_pending_msg4
  ```
  v1 不加（132-862 用戶 full scan 時間 < 50ms 可接受），Phase 2D 觀察 cron P95 timing >= 30s 時才加。

---

## 附錄 A：12 個新 postback action 命名規則

| action | 觸發位置 | 對應 helper |
|--------|---------|------------|
| `intro_next` | 訊息 1「想知道 ABC 怎麼運作」 | handleIntroNext → reply 訊息 2 |
| `intro_maybe` | 訊息 1「我再想想」 | handleIntroMaybe |
| `intro_question` | 訊息 1「我有問題想問」 | handleIntroQuestion → triggerHandoff |
| `method_next` | 訊息 2「想看 12 週怎麼安排」 | handleMethodNext → reply 訊息 3 |
| `method_maybe` | 訊息 2「我再想想」 | handleMethodMaybe |
| `method_question` | 訊息 2「我有問題想問」 | handleMethodQuestion → triggerHandoff |
| `offer_visit` | 訊息 3「想看完整介紹」（**type=URI + ?from=msg3**）| 直跳 /apply landing |
| `offer_maybe` | 訊息 3「我再想想」 | handleOfferMaybe |
| `offer_question` | 訊息 3「我有問題想問」 | handleOfferQuestion → triggerHandoff |
| `final_visit` | 訊息 4「想看完整介紹」（**type=URI + ?from=msg4**）| 直跳 /apply landing |
| `final_maybe` | 訊息 4「我再想想」 | handleFinalMaybe |
| `final_question` | 訊息 4「我有問題想問」 | handleFinalQuestion → triggerHandoff |

**命名規則**：`<段>_<動作>`，段名 = intro/method/offer/final，動作 = next/maybe/question/visit。

**routing table（Phase 2B 重構後）**：
```javascript
const POSTBACK_HANDLERS = {
  // v3.2 新軌
  intro_next: handleIntroNext,
  intro_maybe: handleIntroMaybe,
  intro_question: handleIntroQuestion,
  method_next: handleMethodNext,
  method_maybe: handleMethodMaybe,
  method_question: handleMethodQuestion,
  offer_maybe: handleOfferMaybe,
  offer_question: handleOfferQuestion,
  final_maybe: handleFinalMaybe,
  final_question: handleFinalQuestion,
  // 既有
  q4_story_interested: handleQ4StoryInterested,  // ← v2 改寫成 v3.2 第一步
  q4_story_question: handleQ4StoryQuestion,
  q4_story_maybe: handleQ4StoryMaybe,
  q4_continue: handleQ4Continue,
  q4_decline: handleQ4Decline,
  q4_maybe: handleQ4Maybe,
  handoff_from_q5: handleHandoffFromQ5,
};
// 注意：offer_visit / final_visit 是 URI button，不走 postback handler
```

---

## 附錄 B：訊息範本對照（v3.2 → v3.3）

當前 `草稿_2026-05-03_訊息範本v1.md` 是 v3.2，「介紹 + offer + 早鳥」塞訊息 2 一次發。v3.2 自動推進設計需要拆成 4 段 = 訊息範本 v3.3：

| v3.2 段 | v3.3 段 | path 個人化 | 內容 |
|---------|---------|-----------|------|
| 訊息 1（4 個 path × 故事承接）| **訊息 1**（v3.3 改 textMessage）| ✅ path × 4 | textMessage（無 hero image，剛從學員故事 Flex 點過來不重複）+ 承接故事 + 鋪陳 ABC + 3 postback Quick Reply |
| 訊息 2 上半（嚮導 + 邏輯鏈 + 3,000+ 學員）| **訊息 2**（純介紹 ABC，path 中立）| ❌ 1 版 | 三框架（**A=Add 加營養 / B=Behavior 調體質 / C=Clear 輕負擔**，課程官方定義）+ 引導語「想看 12 週怎麼安排嗎？」+ 3 postback Quick Reply |
| 訊息 2 下半（offer + 24h 早鳥）| **訊息 3**（offer + 早鳥，path 中立）| ❌ 1 版 | 12 週課程結構 + 三段價 + URI 跳 /apply?from=msg3 + 3 Quick Reply（1 URI + 2 postback）|
| 訊息 3（24h follow up + 結果保證）| **訊息 4**（最後提醒，path 中立）| ❌ 1 版 | 結果保證 + 早鳥剩餘時間 + URI 跳 /apply?from=msg4 + 3 Quick Reply（1 URI + 2 postback）|

**Phase 0 前置缺漏**（10.2 已列）：訊息 2/3/4 文案 + 退場 reply 文案，v3.3 範本檔案要建。

---

## 給下個 session 接手的「一句話定位」

> 結單漏斗 v3.2 自動推進契約 v2 寫完了（11 章 + 2 附錄，修了第一輪 yi-challenge 15 個洞）。
> 等 yi-challenge 第二輪挑戰（雙 agent 都找不到新洞才算定版）。
> Phase 0 前置缺：訊息 2/3/4 文案 + 退場 reply 文案（v3.3 範本）需要先寫。
> Phase 2A 動 schema 之前**契約必須定版**。

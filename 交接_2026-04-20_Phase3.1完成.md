# 官方 LINE Bot — 2026-04-20 段落紀錄（A軌 + B軌Phase2b + v6.1 + Phase3.1 全部完成上線）

> **這份給新 session 接手用。**說「接 official-yihugh-line-bot」→ AI 先讀 BRAIN.md → 讀本檔 → 讀 `契約_對話路徑.md` v6.1 → 即可判斷下一步做 Phase 3.2 或其他軌。

---

## 快速脈絡（30 秒看懂）

**今日（04-20）已上線的累計層級**：
1. **A 軌** — 代謝報告結尾三選一 open loop（地雷 / 菜單 / 公斤數），5 種代謝類型個人化
2. **B 軌 Phase 2b** — 對話路徑 DB schema + helpers + /api/health + 41 條模板 seed（全 is_active=false）
3. **v6.1 迭代** — 4 月班 14 位學員自介回饋驅動的 3 個模板調整（q2_path_choice 擴充 + path_b_menopause 新增 + path_e_fallback 共情）
4. **Phase 3.1 MVP dispatch** — webhook/route.js 接 Q1→Q2→Q3 對話路徑（regex 抽體重 + A/B/C/D 選項 + idempotency），**無 AI、TEST_MODE 保護期**

**用戶影響層級**：
- 一般用戶只看到「A 軌代謝報告」的新 open loop（已面向全量用戶）
- 對話路徑 Q1-Q3 **只對白名單（一休 + 婉馨）觸發**（TEST_MODE=true）
- webhook/route.js:47 改 false 就全量開放

---

## 今日累計產出

### 1. A 軌代謝報告 open loop（commit 697ec6a — artemissport session）
**改動**：
- `lib/keywords.js`：加 `trap`（地雷/我的地雷/最大地雷）+ `menu`（菜單/我的菜單/怎麼吃）KEYWORD_RULES + handleTrap/handleMenu
- `lib/keywords.js:METABOLISM_REPORTS` 5 類型全加 `trapTeaser`/`menuTeaser`/`trap`/`menu`
- `app/api/webhook/route.js:TYPE_DATA` 5 類型加 teaser
- `buildPersonalizedReport` 訊息 3 改三選一（地雷 / 菜單 / 公斤數）

**目標**：baseline 5.9% → 15%+ 回訊息率

---

### 2. B 軌 Phase 2b（commit 907c870 — 本 session 前段）

#### migration_008a（schema 擴充）
- `official_line_users` 加 2 欄：`last_stage5_reply_at` / `handoff_rescue_notified`
- `official_reply_templates` 加 3 欄：`label` / `on_keyword_match` / `chain_next_id`
- 新建 `official_webhook_events`（LINE retry idempotency，2 欄 + 2 indexes）
- 新建 `idx_users_stage5_rescue` partial index
- INSERT 22 個 `official_settings`（contract_version='v6' / test_mode / 各閾值）

#### migration_008b（seed 40 條模板）
全部 is_active=false（婉馨 FlexPreview 確認後才啟用）

#### 5 個 lib 新檔（ESM）
- `lib/official-settings-defaults.js` — 22 key JS 常數 fallback
- `lib/official-settings.js` — `getSettingTyped` + `SETTING_SCHEMA`
- `lib/dynamic-templates.js` — `DYNAMIC_TEMPLATE_IDS` + `isDynamic`
- `lib/templates.js` — `renderTemplate`（DYNAMIC 分支 + {diff}/{current}/{target} 變數）
- `lib/users.js` 加 helpers：`mergeByOp` / `updateAiTags` / `updatePathStage` / `getUserPathState`
  - `mergeByOp` 有 ALLOWED_KEYS 白名單擋 AI 幻覺

#### `/api/health?contract=check`
直查 DB 驗證 22 settings + webhook_events 表 + contract_version，不走 getSettingTyped fallback

#### `scripts/test-helpers.mjs`
6 case（後來擴到 38 case）純函式 + DB 實測全過

---

### 3. v6.1 迭代（migration_008c — 本 session 中段）
基於 `ABC瘦身業務/課程內容/4月班學員自我介紹.ini` 14 位學員回饋：

- **q2_path_choice**：選項 B 擴充「或年紀到了代謝變差」+ 末段加動機 open loop「這次想瘦下來，你最想做的第一件事是什麼？」
- **新增 path_b_menopause**（stage=4, condition='menopause_or_age'）— 接住 50+ 更年期族群（14 人裡 4 位主訴，瓊宜/陳靜韋/Rita/Penny）
- **path_e_fallback** 文案改共情領先，mirror 瓊宜原話「嘗試過太多方法…心好累」

模板總數 40 → 41

---

### 4. Phase 3.1 MVP dispatch（commit aede79d + migration_008d — 本 session 後段）

#### code 改動

**`webhook/route.js`**：
- `handleEvent`：加 idempotency（official_webhook_events INSERT，23505 skip）
- TEST_MODE check 移到 idempotency **前面**（避免污染 webhook_events）
- `handleTextMessage` 加 `handleConversationPath` 插入點（matchKeyword 沒匹配才進）
- `handleConversationPath` 分支：
  - `stage<=1` + extractWeights 命中 → 寫 current/target + updatePathStage(2) + 推 q2_weight_× + chain q2_path_choice
  - `stage===2` + isMainChoice(A/B/C/D) → updatePathStage(3, {path}) + 推 path_×_q3
  - 其他 → return false（Phase 3.2 AI 再接）
  - `target >= current` → 推 q1_target_invalid（不 advance stage）

**`lib/conversation-path.js`（新建，純函式）**：
- `CHOICE_TO_PATH` A/B/C/D → healthCheck/rebound/postpartum/eatOut
- `isMainChoice` — 支援小寫 / 全形 / 「選A」/ 前綴「我選」/ 邊界排除「ABCD」「我是 A 類型」
- `extractWeights` — 剔身高 3 pattern（公分/cm、N 身高、身高 N，順序重要）+ current 上限 200（擋身高漏剔）
- `pickWeightDiffCondition` — 三段切（small/medium/large）

**`scripts/test-helpers.mjs`**：38/0 pass
- Case 7-12 含身高混淆真實變體（170 公分 / 165cm / 173 身高 / 身高 180）+ 選項邊界

#### DB 啟用 10 條模板 is_active=true（migration_008d）
- `q1_target_invalid`（改文案：拿掉「正常順序」糾正感）
- `q1_retry_weight`
- `q2_weight_small/medium/large`（large 改文案：拿掉「很大/更糟」當頭一棒）
- `q2_path_choice`（含 v6.1 動機 open loop）
- `path_a_q3` / `path_b_q3` / `path_c_q3` / `path_d_q3`

#### DB 刻意關閉 is_active=false（dead code 防止假啟用）
- `q1_init`（Phase 3.1 沒接 follow 自動推，Phase 3.2 再開）
- `path_e_guide_just_know` / `path_e_fallback`（Phase 3.1 沒 path=other dispatch）

#### gate 結果
- user-eye 🟡：文案層級調整（q2_weight_large / q1_target_invalid 已修）；q2_path_choice 動機 open loop 保留待一休拍板
- yi-challenge 🔴→🟢：身高變體 + dead templates + TEST_MODE 位置全修

---

### 5. LINE 後台自動回應重疊排除（本 session 最後一步）
**背景**：一休之前在 LINE Official Account Manager 設過「提供減脂筆記」自動回應，關鍵字包含 `A/a/B/b/C/c/D/d` 八個單字 → 跟 Phase 3.1 Q2 選項衝突 → 用戶選主因時會先被推「減脂筆記 Flex」後才收到 path_×_q3。

**修復**：一休在 LINE 後台把 8 個單字關鍵字刪掉（保留「透過填寫問卷加入」等合理觸發關鍵字）。已確認無其他 A-D 自動回應衝突。

---

## 線上狀態（現況）

| 項目 | 值 |
|------|-----|
| Repo | `leehugh007/official-yihugh-line-bot` |
| Branch | `main` |
| 最新 commit | `aede79d` Phase 3.1 dispatch |
| Vercel deployment | `dpl_D17CmiHAaqpdgWv4gQnuM3Rz9vJj` = READY |
| Domain | `official-yihugh-line-bot.vercel.app` |
| /api/health?contract=check | `{"ok":true, contract_version_db:"v6", missing_settings:[], webhook_idempotency_table:"ok", test_mode:true}` |
| TEST_MODE | `true`（webhook/route.js:47，白名單 = 一休 + 婉馨）|
| 41 條模板 | 10 條 is_active=true / 31 條 is_active=false |
| 22 個 settings | 全在 |
| official_webhook_events | 有 3 筆 row（實測產生）|

---

## 實測通過的路徑（本 session 一休親測）

### ✅ Q1→Q2 正常流程（22:41 / 22:54）
- 傳：「我目前 170公分，78公斤，想瘦到70公斤」
- Bot 收到：extractWeights 正確抽 `current=78, target=70`（身高 170 正確剔除）
- Bot 推：q2_weight_medium（diff=8 在 5-15 區間）+ q2_path_choice 連推
- DB: `current_weight=78 / target_weight=70 / path_stage=2`

### ✅ Q2→Q3 B 路徑（22:48 / 22:49）
- 傳：「b」（小寫）
- Bot 推：path_b_q3「先問你兩個 — 之前最瘦到幾公斤？維持了多久？…」
- DB: `path='rebound' / path_stage=3 / ai_tags.retry_count_q3=0`

### ✅ Q2→Q3 D 路徑（22:54）
- Reset stage=0 後，再打體重 → 傳「d」（小寫）
- Bot 推：path_d_q3「你平常大致怎麼吃？A 三餐都外面 / B 早午外食...」
- DB: `path='eatOut' / path_stage=3`
- **無 LINE 後台自動回應 Flex 干擾**（已刪重疊關鍵字）

### ✅ 既有關鍵字（A 軌）沒被打壞
- 地雷 / 菜單 / 報告 等 KEYWORD_RULES 優先匹配，不進對話路徑

### ✅ 身高剔除（本地 38 test case + 線上實測）
- 「170公分 78公斤」/「165cm 72公斤」/「173 身高 65」/「身高 180 體重 95」全部正確抽到體重

---

## 尚未啟用 / 尚未完成

### Phase 3.2 待做（AI 層）
- 接 Gemini Flash Lite（契約 8.1 `gemini_model_version='gemini-2.0-flash-lite-001'`）
- Q4 子情境 AI 分類（healthCheck: blood_sugar/cholesterol/blood_pressure/on_meds/no_meds；rebound: stopped/stress/unknown/menopause_or_age；postpartum: time/method/breastfeeding；eatOut: DYNAMIC path_d_ai_meal_feedback）
- `ai_tags` 抽取（痛點 / 猶豫 / 意願 / 關注）
- Q2「其他狀況」自由文字分類（目前只接 A/B/C/D 硬選）
- DYNAMIC 模板（`path_d_ai_meal_feedback` 用 AI prompt 骨架產文案）
- 啟用 Q4 子情境的 ~20 條模板

### Phase 3.3 待做（運營層）
- 5 條 cron processor（drip 保留既有 + 加：stageTimeout / aiTagsFreshness / stage5Rescue / scheduledPushes）
- Handoff notify 婉馨（LINE notify / 全域關鍵字 stage>=2 觸發 / outro on_keyword_match）
- 啟用剩餘模板（退路 retry_weight / reawaken / path_b_retry 等）
- 關 TEST_MODE（上線全量）

### Phase 3 前置契約（兩個缺口，**先補再進 Phase 3.2/3.3**）
檔案：`架構契約/official_Phase3前置_admin_ui_成交歸因.md`
- **缺口 #1**：admin UI 補 `label` / `on_keyword_match` / `chain_next_id` 編輯欄位（婉馨後台看不到這三欄）
- **缺口 #2**：`enrolled_from_path` / `enrolled_at` 成交歸因寫入方案拍板（方案 A Recur webhook / B 手動 mark / C 阿算 DB 同步）

### 一休待拍板（user-eye 🟡 建議）
- **q2_path_choice 末段動機 open loop 是否撤回？**（user-eye 44+ 女性視角認為認知負擔高 + 喧賓奪主，v6.1 定版保留等一休實測後決定）
- path_b_q3 / path_c_q3 拆題（一次問太多）

---

## 禁區（延續契約）

- ❌ fullReset 或任何清除 Supabase 用戶數據
- ❌ 改代謝報告文案（A 軌定版）
- ❌ 改契約不跑 yi-challenge
- ❌ webhook 改 dispatch 骨架前不畫 if/else 結構圖（04-04 教訓）
- ❌ 直接啟用新模板 is_active=true 不跑 FlexPreview（除非一休或 Phase 3 前置 admin UI 到位）
- ❌ 關 TEST_MODE 前沒跑 yi-challenge

---

## Git 狀態

- 最新 commit：`aede79d`
- 2 個 stash 仍在（keywords.js trap/menu 舊備份，A 軌 697ec6a 已覆蓋 → 可 drop）

```bash
git stash list
# stash@{0}: On main: keywords.js trap/menu handler 再次出現 — 暫存讓 rebase 通過
# stash@{1}: On main: keywords.js 未完成 trap/menu handler（handler 沒實作會 build 爆）— Phase 2b push 前暫存
# 可 `git stash drop stash@{0}` 兩次清掉
```

---

## Phase 3.2 開工指令（給新 session）

### 開 ss 時說
> 「接 official-yihugh-line-bot，走 Phase 3.2（AI 層）」

### 新 AI 接到後的動作

1. 讀 `BRAIN.md` 系統全貌 + 官方 Bot 狀態
2. 讀**本檔**（完整今日脈絡）
3. 讀 `契約_對話路徑.md` v6.1：
   - 第 3 章 AI 輸出契約
   - 第 5 章觸發規則（level 0-4 dispatch）
   - 第 6 章 Handoff / Fallback
   - 第 9 章 Data Lifecycle（ai_tags 寫入）
   - 附錄 D DYNAMIC_TEMPLATE_IDS
   - 附錄 E Code Gate 過濾規則
   - 附錄 F Retry 文案
4. 讀 `app/api/webhook/route.js` handleConversationPath（Phase 3.1 現況）
5. 讀 `lib/conversation-path.js`（純函式層）
6. 先**不動** webhook dispatch 骨架（Phase 3.1 已驗證），只在 `handleConversationPath` 的「stage>=3 return false」分支加 AI 接入
7. 建議切法：
   - Phase 3.2a：接 Gemini Q4 子情境分類（最小可行，4 條 path_× AI）
   - Phase 3.2b：加 ai_tags 抽取
   - Phase 3.2c：DYNAMIC path_d_ai_meal_feedback

### Phase 3.2 前置必讀
- `架構契約/official_Phase3前置_admin_ui_成交歸因.md`：Phase 3.2 前要不要先補 admin UI？至少 #2 成交歸因方案要先拍板

### 不要做的事
- 不動 A 軌 code（已線上、已面向全量）
- 不動 Phase 3.1 regex / isMainChoice（已驗證）
- 不動 lib/users.js 既有 helpers（已驗證）
- 不啟用 q1_init / path_e_* 的 is_active（Phase 3.1 決策，Phase 3.2 再看是否要 Follow 時觸發 q1_init）

---

## 實測用 SQL（給一休 / 婉馨實測或 debug）

```sql
-- 查一休 / 婉馨當前對話狀態
SELECT line_user_id, display_name, metabolism_type,
       current_weight, target_weight, path, path_stage,
       last_user_reply_at, ai_tags
FROM official_line_users
WHERE line_user_id IN (
  'U51808e2cc195967eba53701518e6f547',  -- 一休
  'U3edf3d2114ee03ad81cff1fd35c04600'   -- 婉馨
);

-- 查最近 webhook events（idempotency 狀態）
SELECT event_id, processed_at
FROM official_webhook_events
ORDER BY processed_at DESC LIMIT 10;

-- 查模板啟用狀態
SELECT id, path, stage, condition, is_active, chain_next_id,
       jsonb_pretty(on_keyword_match) AS on_kw
FROM official_reply_templates
WHERE is_active = true
ORDER BY stage, id;

-- Reset 測試帳號（只限白名單，測新路徑用）
UPDATE official_line_users
SET current_weight=NULL, target_weight=NULL, path=NULL, path_stage=0,
    path_stage_updated_at=NULL, ai_tags='{}'::jsonb, ai_tags_updated_at=NULL
WHERE line_user_id = 'U51808e2cc195967eba53701518e6f547';
```

---

## 關鍵檔案地圖

```
official-yihugh-line-bot/
├── 契約_對話路徑.md           ← v6.1 主契約（Phase 2b/3 唯一真相來源）
├── 交接_2026-04-20_Phase3.1完成.md  ← 本檔
├── 交接_2026-04-20_並行戰略.md ← 早上 A/B 軌分工交接（已過時但歷史保留）
├── Bot對話設計_2026-04-18_定版.md ← 文案來源
├── CLAUDE.md                  ← 專案上下文 + Schema 對齊表
├── 指揮中心.md                ← 部門架構
├── app/api/
│   ├── webhook/route.js       ← Phase 3.1 dispatch 在這
│   └── health/route.js        ← /api/health?contract=check
├── lib/
│   ├── keywords.js            ← A 軌 METABOLISM_REPORTS + handleTrap/Menu
│   ├── users.js               ← Phase 2b helpers（updateAiTags / updatePathStage / ...）
│   ├── conversation-path.js   ← Phase 3.1 純函式（isMainChoice / extractWeights ...）
│   ├── official-settings.js   ← getSettingTyped
│   ├── official-settings-defaults.js ← 22 key JS fallback
│   ├── templates.js           ← renderTemplate
│   └── dynamic-templates.js   ← DYNAMIC_TEMPLATE_IDS
├── scripts/
│   └── test-helpers.mjs       ← 38/0 pass
└── supabase/
    ├── migration_007_conversation_paths.sql
    ├── migration_008a_conversation_paths_phase2b.sql
    ├── migration_008b_seed_templates.sql
    ├── migration_008c_iteration_from_april_cohort.sql ← v6.1
    └── migration_008d_phase31_iteration.sql          ← Phase 3.1 啟用 + 文案調

架構契約/
├── official_Phase3前置_admin_ui_成交歸因.md ← Phase 3.2/3.3 前要補
└── 踩過的雷.md                ← yi-challenge 必讀

~/.claude/checkpoints/official-yihugh-line-bot/
└── 2026-04-20T21:36-Phase3.1上線.md ← 最新 crash recovery
```

---

*04-20 本 session 完整收尾。新 ss 走 Phase 3.2 前先讀這份 + 契約 + BRAIN.md。*

# V3.2 自動推進漏斗試行測試 SOP（給一休 + 婉馨）

> 建立：2026-05-04
> 部署：commit `b60c8fc`，Vercel Production Ready（2026-05-04 ~01:00）
> 試行範圍：Phase 2A + Session 1 + Session 2 + TEST_ALLOWLIST gate
> 一般用戶：**0 影響**（gate 鎖住，按 q4_story_interested 走原 polite reply + handoff）

---

## 試行 gate 機制（為什麼一般用戶不會誤入新軌）

`app/api/webhook/route.js` 的 `handleQ4StoryInterested` 入口：

```js
if (!TEST_ALLOWLIST.includes(userId)) {
  return await _q4StoryInterestedLegacy(event, userId);
}
```

`TEST_ALLOWLIST`（webhook L91-94）：
- 一休：`U51808e2cc195967eba53701518e6f547`
- 婉馨：`U3edf3d2114ee03ad81cff1fd35c04600`

非這兩個 userId 按「想了解 ABC」→ 走 `_q4StoryInterestedLegacy` = **跟昨天行為 100% 一樣**（polite reply + handoff_reason='q4_story_interested'，fifi 接手）。

**全量開放方式**：移除 `if (!TEST_ALLOWLIST.includes(userId)) { ... }` block，重 deploy。

---

## 測試前置（一次性 — 已生效）

- ✅ Schema：official_line_users 16 個 v3.2 欄位（migration_017 已 prod）
- ✅ handoff.js reasonZh：6 個中文化 + v3.2 進度行
- ✅ Vercel env Q5_APPLY_SIG_MAX_AGE_SEC = 172800（48h）

---

## 測試流程（一休 / 婉馨用自己的 LINE 測）

### 0. 重置自己的對話狀態（讓自己能從 Q1 重跑）

對 official LINE 傳訊息：「85 → 70」（你目前體重 → 目標體重）

預期：webhook 偵測到 Q1 體重格式 → reset path/q3/q4 + 清 v3.2 tracking → reply Q2 weight diff condition。

> **注意**：這條 reset 路徑會跑 `clearV32Tracking`（Session 1 改的），測完一次能重來。

### 1. 走完 Q1 → Q2 → Q3 → Q4

- Q2：選一個 path（health/rebound/postpartum/eatOut）
- Q3：選 condition（小食 / 久坐 / 焦慮等）
- Q4：傳「同意」/ 自由文字 → bot AI 回個人化 final feedback + Quick Reply 三按鈕

### 2. 按 Q4 Quick Reply「想聽聽」

預期：bot 推學員故事 Flex（path 對應 4 個版本）+ Flex 三按鈕「想了解 ABC / 有問題想問 / 我再想想」。

### 3. 按學員故事 Flex「想了解 ABC」

**這是 v3.2 入口**（gate 命中你 = ALLOWLIST → 進新軌）。

預期：
- ✅ bot reply **訊息 1**（path 對應的承接故事 + 鋪陳 ABC，textMessage）
- ✅ Quick Reply 三按鈕：「想知道 ABC 怎麼運作」「我有問題想問」「我再想想」
- ✅ DB `q5_msg1_sent_at` 寫入

> **異常**：如果你看到「請 fifi 跟你聊聊」polite reply → 表示 gate 沒通（你的 userId 不在 ALLOWLIST，或 path=other）。

### 4. 按訊息 1「想知道 ABC 怎麼運作」

預期：
- ✅ bot reply **訊息 2**（純介紹 ABC：A=Add 加營養 / B=Behavior 調體質 / C=Clear 輕負擔 + 3,000 學員）
- ✅ Quick Reply 三按鈕：「想看 12 週怎麼安排」「我有問題想問」「我再想想」
- ✅ DB `q5_msg1_replied_at` + `q5_msg2_sent_at` 寫入

### 5. 按訊息 2「想看 12 週怎麼安排」

預期：
- ✅ bot reply **訊息 3**（12 週課程 + 三段價：$10,400 / $11,400 / $12,600 + 6/1 開學）
- ✅ Quick Reply 三按鈕：「想看完整介紹」（**URI button**）「我有問題想問」「我再想想」
- ✅ DB `q5_msg2_replied_at` + `q5_msg3_sent_at` + `path_stage = 6` 寫入

### 6. 按訊息 3「想看完整介紹」

預期：跳 `/apply` landing page（HMAC signed URL，含 `?from=msg3` query）。

> **Session 3 待補**：visit handler 解析 ?from= 寫 `q5_apply_from_msg`（Phase 2B Session 3 動）。目前 from 會在 URL 上但 visit handler 不寫 DB。**不影響核心測試流程**（landing 顯示正常 / 報名表單能 submit）。

### 7. 訊息 4 怎麼觸發？

訊息 4 設計是 **cron 推**（Phase 2C 才接，目前不會推）。

要測訊息 4 文案，可以手動 SQL 模擬，或等 Phase 2C 接 cron 後測。

---

## 測「退場」路徑（按「我再想想」 / 「我有問題想問」）

### 任一段按「我再想想」

預期：
- ✅ bot reply 對應 maybe 文案：
  - intro_maybe：「好，沒問題。之後想了解再來找我就好，不打擾你。」
  - method_maybe：「OK，有需要再回來看就好。」
  - offer_maybe：「好，你先想，不急。早鳥 24h 內 $10,400，過了會變 $11,400，你心裡有時間規劃就好。」
  - final_maybe：「OK，那這次就先這樣。如果之後想了解再來找我，我都在。」
- ✅ DB `q5_msg{n}_maybe_at` 寫入
- ❌ **不再 cron 推**（永久退場）

### 任一段按「我有問題想問」

預期：
- ✅ bot reply question 文案（指引 fifi 接手）
- ✅ `triggerHandoff(reason='q5_msg{n}_question')` → `path_stage = 5` + push 通知一休 + 婉馨
- ✅ 通知格式新增 `v3.2 進度：訊息 N（…）後問問題` 一行
- ✅ DB `q5_msg{n}_question_at` 寫入

---

## 測 race condition（連按多次按鈕）

對訊息 1 連按「想知道 ABC 怎麼運作」兩次（快速連點）。

預期：
- 第一次：reply 訊息 2（race guard 寫入 q5_msg1_replied_at）
- 第二次：reply「我已經收到你的選擇了～」（race lost guard）

---

## DB 驗證指令（一休跑）

```sql
-- 看自己的 v3.2 tracking 欄位
SELECT
  q5_msg1_sent_at, q5_msg1_replied_at, q5_msg1_maybe_at, q5_msg1_question_at,
  q5_msg2_sent_at, q5_msg2_replied_at, q5_msg2_maybe_at, q5_msg2_question_at,
  q5_msg3_sent_at, q5_msg3_maybe_at, q5_msg3_question_at,
  q5_msg4_sent_at, q5_msg4_maybe_at, q5_msg4_question_at,
  q5_last_pushed_at, q5_apply_from_msg,
  path_stage, handoff_triggered_at, handoff_reason
FROM official_line_users
WHERE line_user_id = 'U51808e2cc195967eba53701518e6f547';  -- 一休 userId
```

---

## 測完之後

### 全部 OK
回我「測完都 OK」→ 我接 Phase 2B Session 3（dispatcher / handleV32FreeText / from 解析）。
全部 wired up + 你再測一輪 → 拆 gate（移除 if (!TEST_ALLOWLIST.includes(userId)) block）→ 全量開放。

### 哪邊壞了
回我具體：
- 哪一步沒推 / 推錯文案 / Quick Reply 沒出來
- DB 對應欄位有沒有寫
- handoff 通知有沒有收到

我修。

---

## 風險回退

如果發現任何用戶被誤導 / 或 Bot 推錯訊息影響真實用戶體驗：

```bash
git revert b60c8fc 7744ce3 9f29f3a 966020a f30e621 8c0222a
git push origin main
```

完整退回 4/30 main HEAD（commit 47f21c3）。

但**理論上**一般用戶 0 影響（gate 鎖住），revert 只在 ALLOWLIST 用戶體驗壞才需要。

---

## 還沒接的（Phase 2B Session 3 + Phase 2C 留給下個 session）

- stage=4/6 dispatcher priority 分支（用戶在 v3.2 軌中傳自由文字 → 走 handleV32FreeText 不走 Q5 classifier）
- handleV32FreeText（意願詞偵測 + negation + 重發 Quick Reply）
- apply/visit ?from= 5 處配套（量測歸因 q5_apply_from_msg）
- Phase 2C cron 段 5/6/7（訊息 2/3/4 24h 推進救援）

這些不影響「按按鈕鏈路」測試。

# 官方 LINE Bot — Phase 3.2a AI 層上線（2026-04-21）

> **這份給新 session 接手用。** 說「接 official-yihugh-line-bot」→ AI 先讀 BRAIN.md → 讀本檔 → 判斷下一步。

---

## 快速脈絡（30 秒看懂）

Phase 3.2a **已上線，TEST_MODE 保護期內**（只有一休 + 婉馨收得到 Bot 回覆）。

**新增能力**：
1. **Gemini 2.5 Flash Lite 接入** — stage=3 用戶回話時呼叫 AI 分類 Q4 子情境
2. **ai_tags 自動抽取** — 痛點 / 猶豫 / 意願 / 關注（中文 key，JSONB 寫入 official_line_users）
3. **全域 Handoff** — stage>=2 用戶打「我要報名 / 多少錢 / 老公反對」→ 自動推 LINE 通知給婉馨 + 一休
4. **禮貌結束** — 任何 stage 打「太貴 / 沒預算 / 沒錢 / 先不用」→ 回禮貌話 + 寫 intent='low'，stage 保持

**尚未啟用**：
- Q4 子情境模板（18 條 path_a/b/c 的 stage=4 row）全部 `is_active=false`
- 一休實測白名單期間用 `[debug]` 回覆看 AI 分類結果；正式啟用等前置 admin UI 做完 + FlexPreview

---

## 線上狀態（現況）

| 項目 | 值 |
|------|-----|
| Repo | `leehugh007/official-yihugh-line-bot` |
| Branch | `main` |
| 最新 commit | `1229360` Phase 3.2a |
| Vercel deployment | Ready（production）|
| Domain | `official-yihugh-line-bot.vercel.app` |
| /api/health?contract=check | `{"ok":true, contract_version_db:"v6", missing_settings:[], webhook_idempotency_table:"ok", test_mode:true}` |
| TEST_MODE | `true`（webhook/route.js:70）|
| Gemini 模型 | `gemini-2.5-flash-lite`（defaults + DB 都同步）|
| GEMINI_API_KEY | Vercel production env ✅（共用阿算那把）|
| 10 條 Q1-Q3 模板 | is_active=true（Phase 3.1 已啟）|
| 18 條 Q4 模板 | is_active=false（等 admin UI + FlexPreview）|
| 其餘模板（retry / rescue / outro）| is_active=false |

---

## 今日產出（Phase 3.2a）

### 新檔
- **`lib/ai-classifier.js`** — Gemini 2.5 Flash Lite 分類器
  - `classifyQ4Condition({path, current, target, userText})` 主入口
  - `validateAiOutput` 契約 3.3 防禦（confidence/conditions enum/ai_tags 正規化）
  - `getValidConditions(stage, path)` — healthCheck 5 / rebound 4 / postpartum 3 個 condition
  - `buildQ4Prompt` — 英文 key schema + 合法 condition 語意 hint + 禁止 markdown
  - callGemini：fetch + AbortController 10s timeout + JSON markdown sanitize
- **`lib/handoff.js`** — Level 0 禮貌結束 + Level 1 全域 Handoff + notify
  - `matchPoliteEnd` / `matchGlobalHandoff`（want_enroll > asked_price > asked_family）
  - `handlePoliteEnd`（帶 `_from_ai:true` 觸發 intent→意願 映射）
  - `triggerHandoff` = stage=5 + 降 intent high→medium + **await** notifyHandoff（不 fire-and-forget）
  - `notifyHandoff` 推一休/婉馨 LINE（符合契約 6.5 格式）
- **`scripts/test-ai-classifier.mjs`** — 31 個單元測試（validateAiOutput 邊界 + Code Gate E2 + handoff 優先序）
- **`supabase/migration_008e_phase32a_model_update.sql`** — gemini_model_version → 2.5-flash-lite

### 改檔
- **`app/api/webhook/route.js`** handleConversationPath
  - 前端加 Level 0 禮貌結束（任何 stage）+ Level 1 全域 Handoff（stage>=2）
  - 新增 `handleStage3ToQ4`：Code Gate E1（字數<3）+ E2（純 emoji/符號）+ AI 重入保護 1h + AI call + 寫 ai_tags + Q4 模板啟用判斷
  - TEST_MODE + 白名單 → Q4 inactive 時推 `[debug]` 回覆（顯示 path/condition/intent/confidence）
- **`lib/users.js`** — ALLOWED_KEYS 新增 `q4_classified_at` / `q4_condition`
- **`lib/official-settings-defaults.js`** — gemini_model_version 預設改 `gemini-2.5-flash-lite`
- **`CLAUDE.md`** — handoff_reason enum 註解補 `want_enroll`

---

## Yi-Challenge 結果（雙 Opus agent 挑戰，7 修已落地）

| # | 類型 | 問題 | 修法 |
|---|------|------|------|
| 1 | 🔴 | Gemini 模型 `gemini-2.0-flash-lite-001` deprecated + thinkingConfig 可能 400 | 改 `gemini-2.5-flash-lite`、拔 thinkingConfig |
| 2 | 🔴 | `handlePoliteEnd` 寫 intent='low' 被吞（缺 `_from_ai`, ALLOWED_KEYS 只認中文 key）| 加 `_from_ai:true` 觸發 EN→ZH 映射 |
| 3 | 🔴 | `notifyHandoff` fire-and-forget 在 Vercel serverless 會被 kill，婉馨收不到 | 改 await（犧牲 ~2s latency 換可靠送達）|
| 4 | 🔴 | Q4 模板 inactive 時每則訊息都重打 Gemini + ai_tags 無界累積 | 加 q4_classified_at 1h 防重入窗口 |
| 5 | 🔴 | 白名單測試期 Q4 inactive 完全靜默 → 一休會誤判 Bot 壞 | TEST_MODE 時推 `[debug]` 回覆顯示分類結果 |
| 6 | 🔴 | Gemini 偶爾回 markdown 包 JSON → parse 爆 | JSON.parse 前剝 ```json ... ``` 包裝 |
| 7 | 🟡 | triggerHandoff 沒降 intent=medium（契約 6.6 步驟 3）| triggerHandoff 加 updateAiTags intent=medium |

---

## 一休實測腳本（TEST_MODE 白名單：一休 `U51808e2cc195967eba53701518e6f547`）

### 前置：Reset stage=0（讓自己回到 Q1 狀態）
進 Supabase SQL Editor：
```sql
UPDATE official_line_users
SET current_weight=NULL, target_weight=NULL, path=NULL, path_stage=0,
    path_stage_updated_at=NULL, ai_tags='{}'::jsonb, ai_tags_updated_at=NULL
WHERE line_user_id = 'U51808e2cc195967eba53701518e6f547';
```

### Case 1：正常 Q1 → Q2 → Q3 → Q4 AI 分類
1. LINE 傳「我目前 170公分，78公斤，想瘦到70公斤」
   → Bot 推 `q2_weight_medium` + `q2_path_choice`（Phase 3.1 既有）
2. 回「B」
   → Bot 推 `path_b_q3`（Phase 3.1 既有）
3. 回「之前停藥就胖回來了，最近壓力也很大」
   → **Bot 推 `[debug] AI 分類成功：path=rebound / condition=stopped 或 stress / intent=medium / confidence=high`**
   → 這是 Phase 3.2a 新行為，證明 AI 接通

### Case 2：驗證 ai_tags 有寫進 DB
```sql
SELECT line_user_id, path, path_stage, ai_tags
FROM official_line_users
WHERE line_user_id = 'U51808e2cc195967eba53701518e6f547';
```
- 期望 `ai_tags` JSONB 含：`痛點: [{value:'壓力大', ...}]`, `意願: 'medium'`, `q4_classified_at`, `q4_condition`

### Case 3：Level 0 禮貌結束（任何 stage）
1. 停在 stage>=1 的任何狀態
2. LINE 傳「太貴了沒預算」
   → Bot 回「好的，了解了，如果未來想聊再來找我就好，不打擾你。」
3. SQL 驗證 `ai_tags.意願 = 'low'`

### Case 4：Level 1 全域 Handoff（stage>=2 才觸發）
1. 先走到 stage>=2（Q1 完成、打過體重）
2. LINE 傳「我要報名」
   → Bot 回「好的，這邊我先請婉馨老師私下跟你聊會比較仔細〜她會主動找你哦。」
   → 一休 + 婉馨的 LINE 都會**另外**收到一則 `🔴 高意願用戶進 Handoff` 通知（含 admin 連結）
3. SQL 驗證：
```sql
SELECT line_user_id, path_stage, handoff_reason, handoff_triggered_at
FROM official_line_users
WHERE line_user_id = 'U51808e2cc195967eba53701518e6f547';
```
- 期望 `path_stage=5, handoff_reason='want_enroll', handoff_triggered_at=<剛才>`

### Case 5：AI 重入保護（1h 內第二次回 Q3 不會重打 AI）
1. 接 Case 1 的 stage=3 狀態（已分類過）
2. 再傳任何 Q3 回覆
   → Bot 推 `[debug] 跳過 AI（1h 內已分類為 <condition>）...`（不花 Gemini 費用）

---

## 尚未完成（Phase 3.2 後續 + 3.3）

### 前置缺口（Phase 3.2c / 3.3 前必補）
- **前置 #1 admin UI 補 3 欄**（label / on_keyword_match / chain_next_id 編輯介面）→ 婉馨啟 Q4 模板前必有
- **前置 #2 成交歸因**（`enrolled_from_path` / `enrolled_at`）→ 方案 A/B/C 本週拍板（檔案：`架構契約/official_Phase3前置_admin_ui_成交歸因.md`）

### Phase 3.2c 待做（DYNAMIC path_d_ai_meal_feedback）
- eatOut 路徑的 stage=3→4 不走 enum 分類，走 AI 動態生成 feedback_text
- 需加 `lib/dynamic-prompts.js` 或擴充 ai-classifier.js
- 需加 Code Gate E6（meal_min_chars + 圖片偵測）
- 啟用 `path_d_ai_meal_feedback` 單一 DYNAMIC 模板

### Phase 3.3 待做（運營層）
- 5 條 cron processor（drip 既有 + 新增 stageTimeout / aiTagsFreshness / stage5Rescue / scheduledPushes）
- Handoff 1h 去重邏輯（契約 6.6 v6）
- 啟用剩餘模板（path_×_retry / reawaken / path_b_retry / outro 類）
- **關 TEST_MODE**（上線全量）

### 一休待拍板
- **Q4 模板啟用時程**：等 admin UI + FlexPreview 機制 ready 才分批開
- **成交歸因方案**：A（Recur webhook 自動）/ B（後台手動 mark）/ C（阿算 DB 同步）

---

## 禁區（延續契約）

- ❌ fullReset / 清除 Supabase 用戶數據
- ❌ 改代謝報告文案（A 軌定版）
- ❌ webhook 改 dispatch 骨架前不畫 if/else 結構圖（04-04 教訓）
- ❌ 直接啟用新模板 is_active=true 不跑 FlexPreview（除非一休或前置 admin UI 到位）
- ❌ 關 TEST_MODE 前沒跑 yi-challenge
- ❌ 改契約不跑 yi-challenge

---

## Git 狀態

- 最新 commit：`1229360` — feat: Phase 3.2a 對話路徑 AI 層
- 變更：+884 / -3，新檔 4 支、改檔 3 支
- 已 push、已部署、已驗證 health

---

## Phase 3.2c 開工指令（給下個 session）

### 說
> 「接 official-yihugh-line-bot，走 Phase 3.2c（eatOut DYNAMIC path_d_ai_meal_feedback）」

### 動作
1. 讀 BRAIN.md
2. 讀**本檔**
3. 讀 `契約_對話路徑.md` v6.1 第 4.2 章（DYNAMIC 規則）+ 附錄 D + 附錄 E（Code Gate E6/E6'）
4. 讀 `lib/ai-classifier.js`（抄 callGemini pattern）
5. 讀 `app/api/webhook/route.js` `handleStage3ToQ4` 的 path=eatOut 分支（目前是 `return false` 靜默）
6. 實作：
   - `lib/ai-classifier.js` 加 `generateMealFeedback({current, target, userText})` 動態生成函式
   - `handleStage3ToQ4` eatOut 分支接 generateMealFeedback → renderTemplate（DYNAMIC）→ reply
   - Code Gate E6：字數 < meal_min_chars + 無圖片 → 推 path_d_retry_meal_detail
   - Code Gate E6'：在 `handleEvent` message type='image' 分支加（非 handleTextMessage）

### 不要做的事
- 不動 Phase 3.2a 已上線的 code（healthCheck/rebound/postpartum 分類）
- 不動 Phase 3.1 regex / isMainChoice / extractWeights
- 不啟用 path_d_ai_meal_feedback 的 is_active 前未跑 FlexPreview

---

*2026-04-21 Phase 3.2a 完整落地。下個 session 視一休實測結果決定是否直接進 3.2c 或先做前置缺口。*

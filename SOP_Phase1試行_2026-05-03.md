# Phase 1 試行 SOP（婉馨手動跑用）

> 建立：2026-05-03
> 對應：訊息範本 `草稿_2026-05-03_訊息範本v1.md`（v3.2）+ 提案 `提案_2026-04-30_結單漏斗v3.md`
> 目的：5-10 個用戶手動跑完整 v3.2 流程，量轉換、抓盲點，再決定是否動 code（Phase 2A）
> 階段：純文案、零 code 動

---

## 1. 試行目標 + 觀察期

### 量什麼

跟過去 fifi monologue（婉馨自己想話接）的 baseline 比，看 v3.2 訊息範本 + 階梯式 Quick Reply 引導**有沒有改善**這四段轉換：

| 漏斗段 | baseline 期待 | v3.2 期待 |
|--------|--------------|-----------|
| 訊息 1 → 用戶回字（願意下一步） | ~30%? 沒精算過 | 量出來 |
| 用戶回字 → /apply 連結點擊 | ~50%? | 量出來 |
| /apply 點擊 → submit 完成 | 過去 1-2% bot 整體；新 landing 待量 | 量出來 |
| 訊息 1 → submit（端到端） | 過去 1-2% | 量出來 |

### 觀察期

- 跑 **5-10 個** q4_story_interested 用戶就停下來看
- 跑完一輪覆盤一次，**不要等到 30 個再回頭**（樣本小，每個用戶都要看仔細）
- 每天跑 1-3 個就夠，不急

### 退出條件

任一情況立刻停 + 回報一休：
- 連續 3 個用戶推完訊息 1 全靜默（沒回字也沒按按鈕的反向意圖）
- 用戶明顯反感（回「不要再傳了」「太煩」之類）
- 訊息範本被某個 path 撞到尷尬情境（婉馨記下哪個 path × 哪個情境）

---

## 2. 撈用戶清單

### 阻塞點

admin 後台用戶 tab 只能篩 segment / source / tag，**沒有 path_stage / handoff_reason 篩選**。婉馨無法自己從 admin 撈到 q4_story_interested 名單。

### 解法（Phase 1 期間）

**一休或 AI 用 Supabase MCP 撈，匯給婉馨**。試行期間每次開工前跑一次。

#### SQL 範本

```sql
SELECT
  line_user_id,
  display_name,
  path,
  ai_tags->>'q3_condition_selected' AS q3_condition,
  current_weight,
  target_weight,
  metabolism_type,
  handoff_triggered_at,
  handoff_reason,
  ai_tags->'痛點' AS pains
FROM official_line_users
WHERE path_stage = 5
  AND handoff_reason = 'q4_story_interested'
  AND blocked_at IS NULL
ORDER BY handoff_triggered_at DESC
LIMIT 10;
```

> Phase 1 沒有 `q5_intro_sent_at` 欄位（Phase 2A migration 才加）。Phase 1 用 google sheet 自己標「已試行過」excluded list，下次 SQL 跑完手動排除已處理的 line_user_id。

#### 匯出流程

1. AI 跑 SQL → 結果貼到 google sheet `Phase1試行追蹤_2026-05`（一休先建一個）
2. sheet 的 row order = 婉馨的執行順序
3. 婉馨開工時看 sheet，從上往下挑沒處理過的 row

---

## 3. 訊息 1：依 path 選範本

### 對照表

| `path` 欄位 | 範本（v3.2 訊息範本檔案內 section） | 學員主角 |
|------------|--------------------------------|---------|
| `healthCheck` | #### healthCheck（剛看完沛蓁的故事） | 沛蓁 |
| `rebound` | #### rebound（剛看完俐臻的故事） | 俐臻 |
| `postpartum` | #### postpartum（剛看完溫溫的故事） | 溫溫 |
| `eatOut` | #### eatOut（剛看完美美的故事） | 美美 |
| `other` / NULL | ### 通用 Fallback（path = other / null 時） | （無學員主角） |

**範本位置**：`official-yihugh-line-bot/草稿_2026-05-03_訊息範本v1.md` 訊息 1 章節（4 個 path 版本 + Fallback）

### 婉馨操作流程

1. 看 google sheet 該 row 的 `path` 欄位
2. 打開訊息範本檔案，找對應 section
3. 複製整段文字（包含 Quick Reply 模擬部分，見下節）
4. 貼到該用戶的 LINE 對話
5. sheet 標記「訊息 1 已推 ✓」+ 推送時間 + path

---

## 4. Quick Reply 文字模擬（Phase 1 沒按鈕，用回字「1」「2」代替）

Phase 2A 寫 code 後 Quick Reply 是真按鈕。Phase 1 階段婉馨手動，**訊息 1 結尾用文字代替按鈕**：

### 訊息 1 結尾（每個 path 都加這段）

```
———
想知道 ABC 怎麼運作 → 回我「1」
我再想想 → 回我「2」
```

### 為什麼用「1」「2」不直接寫「想了解」「再想想」全文

- 簡單、用戶最快回（一個字）
- 跟 Phase 2A Quick Reply 結構同型（按鈕也是兩個選項），測試結果有可比性
- 萬一用戶回「想了解」「OK」「好」這種開放回覆，婉馨依**自由文字判斷**走哪一支：
  - 表達正面意願 → 等同回「1」
  - 表達猶豫 / 「再看看」/「考慮」 → 等同回「2」
  - 完全離題（傳貼圖、說「謝謝」、回不相關） → 對話例外處理表（章節 7）

---

## 5. 用戶回應分流（婉馨怎麼判斷推哪一段）

### 流程圖

```
推訊息 1
  │
  ├── 用戶回「1」/ 正面意願（24h 內）
  │     → 立即推訊息 2（24h 早鳥 $10,400 + StoryBrand）
  │     → sheet 標「訊息 2 已推 ✓」+ 時間
  │
  ├── 用戶回「2」/ 表達猶豫
  │     → 不推訊息 2、不推訊息 3
  │     → sheet 標「我再想想」+ 進「nurturing 候選池」（v4 設計用，Phase 1 先記住）
  │
  ├── 用戶回字但不相關（貼圖、「謝謝」、離題）
  │     → 對話例外處理（章節 7）
  │
  └── 用戶 24h 沒回字也沒回任何訊號
        → 推訊息 3（常規早鳥 $11,400 + 結果保證）
        → sheet 標「訊息 3 已推 ✓」+ 時間
```

### 關鍵時間點

| 觸發 | 動作 |
|------|------|
| 推訊息 1 後 | 看用戶**今天**有沒有回，沒回**先放著**，明天再判斷 |
| 訊息 1 推完滿 24h 沒任何回應 | 推訊息 2 + 訊息 3 之間擇一？**選訊息 3**（沒互動 = 24h 早鳥已過時，直接結果保證 + 常規早鳥更合理） |
| 用戶回「1」後推訊息 2，再 24h 沒點 /apply | **不主動再追**（讓用戶優雅退出，不過度騷擾） |
| 用戶按 /apply 連結但沒 submit | sheet 標「點了沒報名」+ **不追**（v4 nurturing 設計來解，Phase 1 不解） |

### 24h 沒回的延伸判斷

一休原則「按鈕引導不靠開放打字」。如果用戶沒按沒回 = **意圖不夠強**，不要硬追。
- 推訊息 3 是給最後一次機會
- 訊息 3 也沒回 → **進 nurturing 候選池**，Phase 1 不再主動推

---

## 6. 訊息 2 / 訊息 3 範本位置

### 訊息 2

`草稿_2026-05-03_訊息範本v1.md` 訊息 2 章節（StoryBrand 完整版）

**Phase 1 結尾改成文字模擬**（取代 Quick Reply 按鈕）：

```
———
想看課程完整介紹 → 點這個
https://official-yihugh-line-bot.vercel.app/apply

我再想想 → 回我「2」
```

### 訊息 3

`草稿_2026-05-03_訊息範本v1.md` 訊息 3 章節（結果保證 + 常規早鳥）

**Phase 1 結尾同訊息 2**：

```
———
想看課程完整介紹 → 點這個
https://official-yihugh-line-bot.vercel.app/apply

我再想想 → 回我「2」
```

---

## 7. 對話例外處理

直接照訊息範本「對話例外處理」章節處理。摘要：

| 用戶情況 | 婉馨怎麼接 |
|---------|----------|
| 用戶按「想了解」但沒回字 24h 以上 | 推訊息 3（同章節 5） |
| 用戶回字但不太相關（貼圖、「謝謝」） | 用訊息 2 通用版 + 加一句「方便的話可以告訴我你最在意什麼，我幫你看角度」 |
| 用戶直接問價格 | 跳過訊息 2，直接給 /apply 連結 + 24h 早鳥資訊 |
| 用戶問「這跟阿算有什麼不同」 | 標準回答 — 阿算是免費 LINE Bot，ABC 課程是 12 週系統性服務，兩個不同層次 |
| 用戶說「我先想想」 | 標準回答 — OK 你先看，早鳥 24h 內 $10,400，過了 $11,400 |
| 用戶情緒崩潰 | **不要立刻推 /apply**。先承接情緒。今天先不講課程，問用戶最近怎麼了 |
| 用戶比較外部競品 | 不評論別人。給 abcmetabolic.com/method 30 秒看完 |
| 用戶報名後問匯款 | 推匯款資訊（既有 SOP）+ 確認姓名電話 |

---

## 8. Google Sheet 追蹤表欄位 spec

一休先建 google sheet `Phase1試行追蹤_2026-05`，欄位如下：

| 欄位 | 類型 | 填寫時機 | 說明 / 來源 |
|------|------|---------|------|
| line_user_id | 字串 | SQL 撈出時 | 從章節 2 SQL 結果填 |
| display_name | 字串 | SQL 撈出時 | LINE 顯示名 |
| path | 字串 | SQL 撈出時 | healthCheck/rebound/postpartum/eatOut/other |
| q3_condition | 字串 | SQL 撈出時 | 參考用，Phase 1 不影響範本選擇 |
| handoff_triggered_at | 時間 | SQL 撈出時 | 進高意願池時間 |
| 訊息 1 推送時間 | 時間 | 婉馨推完 | YYYY-MM-DD HH:mm |
| 用戶回字時間 | 時間 | 用戶回時 | 沒回填空（婉馨從 LINE 對話看） |
| 用戶回字內容 | 字串 | 用戶回時 | 完整貼用戶說了什麼（短句、表情都算） |
| 訊息 2 推送時間 | 時間 | 婉馨推完 | 沒推填空 |
| /apply 點擊時間 | 時間 | 一輪結束時補 | 用 SQL 撈 `q5_clicked_at`：`SELECT q5_clicked_at FROM official_line_users WHERE line_user_id IN (...)` |
| 訊息 3 推送時間 | 時間 | 婉馨推完 | 沒推填空 |
| 是否 submit | bool | 一輪結束時補 | 用 SQL 撈 `path_stage=8`（或 admin 報名 tab 看 line_user_id 有沒有筆 application） |
| submit 時間 | 時間 | 同上 | 用 SQL 撈 `enrolled_at` |
| 結果分類 | enum | 一輪結束 | submitted / clicked_no_submit / replied_no_click / no_reply / opted_out / exception |
| 備註 | 字串 | 隨時 | 婉馨觀察的尷尬點、特殊回應 |

> **Phase 1 不要求 admin 即時顯示這些欄位**。婉馨手動跑時只填得到「訊息推送時間 + 用戶回字內容」這幾欄；點擊時間 / submit 狀態這類「系統側」欄位**一輪結束時用 SQL 一次補完**，不必每天追。

### 結果分類定義

- **submitted**：完成報名（最佳結果）
- **clicked_no_submit**：點了 /apply 但沒填表（v4 nurturing 對象）
- **replied_no_click**：回字了但沒點 /apply（測訊息 2 文案是否打中）
- **no_reply**：訊息 1 推完完全沒反應（測訊息 1 開場是否抓得住）
- **opted_out**：用戶明確說「我再想想」/「不需要」
- **exception**：對話例外處理（情緒、競品比較、跑題）

---

## 9. 一輪覆盤回報模板（5-10 個用戶後）

跑完一批後，婉馨/AI 整理一份回報給一休：

```
# Phase 1 試行第 N 輪覆盤（YYYY-MM-DD ~ YYYY-MM-DD）

## 樣本
- 總用戶：N
- path 分布：healthCheck X / rebound Y / postpartum Z / eatOut W / other V

## 漏斗轉換
| 段 | 人數 | 轉換率 |
|---|------|-------|
| 訊息 1 推送 | N | 100% |
| 用戶回字 | A | A/N |
| /apply 點擊 | B | B/A（從回字算）|
| submit 完成 | C | C/B（從點擊算）|
| 端到端 | C | C/N |

## path 表現對照
| path | 樣本 | 回字率 | 點擊率 | submit 率 | 觀察 |
|------|------|-------|-------|----------|------|
| healthCheck | ... | ... | ... | ... | ... |
| ...

## 範本表現亮點 / 痛點
- 哪幾個 path 範本打得到 → 證據
- 哪幾個 path 範本撞牆 → 文字哪一段沒接住

## 對話例外發生頻率
- 情緒崩潰：N 次
- 競品比較：N 次
- 跑題：N 次

## 建議下一步
- [ ] 範本 v3.3 修哪幾段
- [ ] Phase 2A migration 是否啟動
- [ ] v4 nurturing 設計優先級
```

---

## 10. 限制 / 不要做什麼

> 取自交接指令 + checkpoint，明確列出 Phase 1 階段 **不要做** 的事。

- ❌ **不要動 code**（14 個銜接洞 + schema 對齊未解，動 code = 高風險）
- ❌ **不要改訊息範本 v3.2**（除非一休給意見）
- ❌ **不要把阿算（abc-line-bot）綁進課程介紹**（章節 7 標準回答以外不要延伸）
- ❌ **不要寫「準備期 LINE 群組」**（沒群組 = 假承諾）
- ❌ **退費對外不要寫具體條件**（「按課程進度」就停，「每天上傳飲食紀錄」是內部標準不公開）
- ❌ **不要疊加雙重急迫話術**（「最後 5 名 + 5/10 截止」用戶會覺得是話術）
- ❌ **不要追沒回字也沒按按鈕的人超過訊息 3**（過度騷擾，留給 v4 nurturing 設計）
- ❌ **不要假設「q4_story_interested」用戶都知道學員故事是誰**（婉馨選範本前先看 path 對應的學員，避免張冠李戴）

---

## 11. 接續步驟

### 第一輪試行條件（要先準備好才能開始）

- [x] 訊息範本 v3.2 定版
- [x] SOP（本檔）
- [ ] google sheet `Phase1試行追蹤_2026-05` 建好（一休或婉馨建）
- [ ] 一休 review 本 SOP + 訊息範本 v3.2，給 OK
- [ ] AI 跑第一次 SQL 撈名單（≤10 人）填到 sheet
- [ ] 婉馨清楚知道操作流程（章節 3-7）

### 跑完第一輪後

1. 寫覆盤（章節 9 模板）
2. 一休判斷下一步：
   - **A**：範本表現好 → 直接進 Phase 2A migration（補 P0 1-5 銜接洞 spec → 動 schema）
   - **B**：範本要修 → 寫 v3.3 → 第二輪試行
   - **C**：v4 nurturing 優先 → 補 nurturing spec
   - **D**：基礎結構有問題 → 退回提案，重新設計

---

## 12. 給下次 session 接手的「一句話定位」

> Phase 1 試行 SOP 寫完了，等一休 review + 建 google sheet + 撈第一批 ≤10 用戶。
> 婉馨用 SOP 章節 3-7 操作，章節 8 記錄。第一輪跑完用章節 9 覆盤。
> Phase 2 動 code 之前**先看第一輪數據**再決定是否啟動 migration_017/018/019。

# 官方 LINE Bot — Phase 4.0 完成交接（2026-04-23）

> **新 session 開工第一件事：讀這份 + `用戶旅程.md` + `契約_Q5轉換漏斗.md` v2.3**
> 不要再問流程，文件都寫死了。跟 code 有出入以 code 為準 + 立刻更新文件。

---

## 一句話現況

Phase 4.0（Q5 前置基建）全收工、TEST_MODE=false 上線 prod、Phase 4.1 待啟動。

---

## 當前 prod 真實狀態（寫死，不用猜）

| 項目 | 值 | 位置 / 備註 |
|------|-----|------------|
| TEST_MODE | **`false`** | route.js:72（2026-04-23 PR #29 切）|
| B 軌 Q1→Q4 | ✅ 全量開放 | Phase 3.3 邏輯（healthCheck/rebound/postpartum/eatOut/other 五條 path） |
| Q5 軟邀請 | ❌ 未 wire | Phase 4.2 才接 classifier + performQ5Transition |
| /apply 頁 | ✅ live（簡化版 LIFF） | PR #30。只做 LIFF init，不驗 userId，不做 redirect |
| handoff notify | ✅ push 給一休+婉馨 | lib/handoff.js:16-19（寫死，後台可改 `handoff_notify_to`）|
| Supabase | q5_* 5 欄 + blocked_at 已建 | migration_011 + 012 已跑（ref `fnlkhxnfaylhqhystmbr`）|
| Supabase MCP | ✅ 可直用 | `mcp__supabase__execute_sql`，project_id `fnlkhxnfaylhqhystmbr` |
| Main HEAD | PR #30 squash commit | fix/apply-liff-loop |
| 開的 PR | 無（全 merged 或已 close）| |

### LIFF 配置

- LINE Login channel：`apply-liff`（Provider「特懿學股份有限公司」下）
- LIFF app name：`apply-landing`
- LIFF ID：`2009872928-plrAZYbN`
- LIFF URL：`https://liff.line.me/2009872928-plrAZYbN`
- Endpoint URL：`https://official-yihugh-line-bot.vercel.app/apply`
- Size：Full / Scopes：profile / Add friend：Off
- Linked OA：「@621tkxhy / 一休-陪你健康瘦一輩子」已綁
- Vercel env：`NEXT_PUBLIC_LIFF_ID=2009872928-plrAZYbN`（Production + Preview）

---

## 用戶流程（不要再問，讀完這份 + 用戶旅程.md 就清楚）

```
abcmetabolic.com 代謝測驗 → 拿代碼
  ↓
LINE 加好友「一休陪你健康瘦」（@sososo）
  ↓
傳 4 碼代碼 → handleCodeClaim 個人化代謝報告（繞 TEST_MODE）
  ↓
打「菜單」或「地雷」→ matchKeyword 命中 → A 軌自動回覆（繞 TEST_MODE）
  ↓ ★ A→B 銜接鉤子：回覆 template 末尾問「告訴我你目前幾公斤、想瘦到幾公斤」
  ↓ （⚠️ 這是 Bot 自動，不是婉馨人工）
用戶回「50.9 47」
  ↓
handleConversationPath → extractWeights → 進 Q2（體重差分流）
  ↓
Q2 選 A/B/C/D → 分到對應 path（healthCheck/rebound/postpartum/eatOut/other）
  ↓
Q3 選 1/2/3/4 子情境
  ↓
Q4 AI 綜合回饋（Gemini generateFinalFeedback，視角：我=一休/你=用戶）
  ↓
stage=4（目前 Bot 就靜默了，Q5 Phase 4.2 才 wire）
```

### handoff pre-check（任何 stage≥2 用戶都會觸發）

- 關鍵字：「想報名 / 多少錢 / 我老公反對 / ...」
- 觸發 → stage=5 + push 通知一休+婉馨

---

## Phase 4.0 10 條 PR 歷程（簡）

| PR | 內容 |
|----|------|
| #21 | Q5 轉換漏斗契約 v2.3 定版（5 輪 yi-challenge 修 30 Critical + 3 Minor）|
| #22 | blocked_at 欄位 + markBlocked 寫時間戳 |
| #23 | webhook case 'postback' + handoff_from_q5（本 session 手動解 conflict）|
| #24 | case 'message' pre-check（stage 6/7 非文字→軟 handoff）+ 3 handler state 簽名 |
| #25 | lib/q5-state.js helper（atomic UPDATE race guard）+ migration_012 + verify script + vitest |
| #26 | /apply LIFF 原版（merged 後發現桌面 LINE loop + userId namespace 問題）|
| #27 | rollback policy：pushFn 失敗不清 q5_sent_at（避免 cron 重推）+ pre-check q5_sent_at guard |
| #28 | 用戶旅程.md + CLAUDE.md 啟動必讀 pointer + BRAIN.md 更正 Supabase 共用事實 |
| #29 | TEST_MODE=false（B 軌 Q1-Q4 全量開放）|
| #30 | /apply LIFF 簡化版（移除 isInClient redirect 防 loop + 移除 userId 比對因 LINE 2024 policy）|

---

## Phase 4.1 啟動（新 session 接這個）

### 範圍

1. **Migration 013**：`official_program_applications` 表 + 剩餘 q5_* 欄位（q5_click_count / q5_clicked_at / q5_visit_followup_sent_at）+ partial indexes
2. **SETTING_SCHEMA 三處同步**：
   - `lib/official-settings.js` SETTING_SCHEMA 加 15 個 key（見契約 Ch.8）
   - `lib/official-settings-defaults.js` 加預設值
   - `app/admin/page.js:2189` SETTING_LABELS 加兩個 Q5 文案 key + label
3. **/api/apply/visit**：POST endpoint 記錄訪問（stage 6→7 + q5_clicked_at COALESCE + q5_click_count++）
4. **/api/apply/submit**：POST endpoint 走 `submit_application` RPC（PL/pgSQL function，含 `IF NOT FOUND RAISE P0002`）
5. **🆕 HMAC signed URL 驗 server 側**（取代 LIFF 做不到的 anti-造假）：
   - Bot 推 Q5 時生成：`/apply?userid=X&sig=<HMAC(userid+ts+secret)>&ts=<unix_ts>`
   - /api/apply/submit 驗 sig + ts（24h 過期）
   - 不解 LINE-to-LINE 分享（Phase 4.5 若污染再加手機驗證碼）
6. **/apply SSR**：landing 五章 copy 套進 `app/apply/page.js`，參考 `apply頁landing規劃.md`
   - 渴望 / 可能性 / 確定感 / 心理錨點轉換 / 方案+報名
   - 保留 PR #30 的 LIFF init 骨架
7. **yi-challenge #6 test stateful mock**：改寫 `__tests__/q5-state.test.js` 用 queue pattern 驗 rollback 路徑（或 scripts/verify-q5-atomic.js 加 `--test-rollback` mode）

### 動手前置

- Supabase MCP 可直用（project_id `fnlkhxnfaylhqhystmbr`）跑 migration_013
- /apply 頁 landing copy 由一休寫或 yi-voice skill 產（先討論再動筆）
- HMAC secret 走 Vercel env，新 key `Q5_APPLY_SIGNING_SECRET`

### 不要做

- ❌ 不改 Q1-Q4 狀態機（契約 v6.1 邊界）
- ❌ 不改主動軌文案（已定 B1d「之前跟你聊到的那些卡關」）
- ❌ 不加 UNIQUE 到 `official_program_applications`（允許家庭共用 LINE）
- ❌ Q5 訊息不講價格/方案細節（決策 B）
- ❌ 不跳過 atomic 驗證 SOP（migration_013 建新 UPDATE 路徑前跑 verify-q5-atomic）
- ❌ 不改 q5_intent 寫入路徑（只能由 `lib/q5-state.js:updateQ5Intent` 寫）
- ❌ 不 hardcode `phase_4_2_launch_date`（存 official_settings）

---

## Phase 4.2 後續（4.1 完成後）

- `lib/q5-classifier.js`（5 error path 全寫 ai_failed）
- `lib/q5-message.js`（pushMessage + Quick Reply 兩按鈕）
- webhook stage=4 分支接 Q5 classifier + performQ5Transition
- handoff.js stage=6/7 專屬文案（Ch.6.1.1）
- Cron `q5-maintenance`：主動軌 + visit-followup + reset + notify retry
- 補推舊 333 被擋用戶（一休同意 Q5 完整後才推）

---

## Phase 4.5 觀察期

- baseline 一週 + 95% 置信區間
- 真 click rate 偏低 → A/B 主動軌文案
- applications 重複 phone/email 嚴重 → 加手機驗證碼
- TEST_MODE 搬 official_settings（後台可開關）

---

## 新 session 開工指令（直接複製貼上）

```
接官方 LINE Bot Phase 4.1 — 先讀這 3 份：

1. official-yihugh-line-bot/用戶旅程.md（A 軌→B 軌銜接鉤子、當前開放範圍、斷點）
2. official-yihugh-line-bot/交接_2026-04-23_Phase4.0完成.md（這份，完整現況）
3. official-yihugh-line-bot/契約_Q5轉換漏斗.md v2.3（Phase 4.1 規格唯一真相）

Phase 4.0 已全收工（TEST_MODE=false 上線、/apply LIFF 簡化版 live、migration_011+012 跑好）。

啟動 Phase 4.1：
1. migration_013（applications 表 + 剩餘 q5_* 欄位）— MCP 可直用（project_id fnlkhxnfaylhqhystmbr）
2. SETTING_SCHEMA + defaults + SETTING_LABELS 三處同步
3. /api/apply/visit + /api/apply/submit + submit_application PL/pgSQL RPC
4. 🆕 HMAC signed URL（Bot 推 Q5 帶 sig+ts，/api/apply/submit server 端驗）— 取代 LIFF userId 做不到的 anti-造假
5. /apply landing 五章 copy（先跟一休討論選角度 + yi-voice 審）
6. yi-challenge #6 test stateful mock 補

紅線：
- 不改 Q1-Q4 狀態機
- 不改主動軌文案（B1d 定版）
- 不加 UNIQUE 到 applications（家庭共用 LINE）
- Q5 訊息不講價格/方案
- 不跳過 atomic 驗證 SOP
- 不改 q5_intent 寫入路徑（只由 lib/q5-state.js:updateQ5Intent 寫）

動手前跑 /yi-safe-change refresh。每改一支 code 就 build 驗證 + commit。
```

---

## 給新 AI 的附加提醒

1. **Supabase 三 Bot 共用一個專案**（ref `fnlkhxnfaylhqhystmbr`，顯示名 coach-line-bot 是歷史命名）。MCP `execute_sql` 直接用，不用寫本地 tmp script。
2. **LIFF userId 跟 URL userid 是不同 namespace**（LINE 2024 policy），不要再試著做 userId 比對。
3. **Q5 欄位寫入唯一入口是 `lib/q5-state.js`**，不要 inline SQL（契約 Ch.0.3 寫死）。
4. **一休 LINE userId**：`U51808e2cc195967eba53701518e6f547`（白名單 + 測試用）。
5. **handoff notify 會 push 給一休+婉馨**（TEST_MODE=false 後是正常行為）。
6. **action 前寫 task-signal.json + 每 15 分鐘 refresh /yi-safe-change**（hook 會擋）。
7. **yi-safe-change 紅線「一個 session 不超過 3 個功能點」**。Phase 4.1 七項任務建議拆 2-3 個 session 做，不要硬撐。

---

*Phase 4.0 歷時約 8 小時（2026-04-22 晚到 2026-04-23 中），10 條 PR，3 次 yi-challenge（共抓 7 Critical），1 次實測 loop bug。下 session 從 Phase 4.1 開始。*
